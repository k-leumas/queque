---
context: default
phase: null
task: null
total_tasks: null
status: paused
last_updated: 2026-06-04T23:13:55.000Z
---

# Session Handoff — Homebrew Binary Fix + Tooling Cleanup

No blocking constraints.

<current_state>
Fix ready to commit and push: `src/cli/main.ts` — `isDirectRun` used `process.argv[1]` (symlink path) vs `import.meta.url` (real Cellar path) — they never matched so `main()` was never called from the Homebrew binary. Fix: wrap `process.argv[1]` in `fs.realpathSync()` before comparing. This is the root cause of production `??` doing nothing (inline/non-Zellij path).

`README.md` also updated this session (Homebrew install flow + API key resolution section).

Uncommitted files: `src/cli/main.ts`, `README.md`, `.wolf/*`
</current_state>

<completed_work>

- Fixed `package.json` `prepare` script: `[ -d .git ] && lefthook install || true` — prevents npm install failing in Homebrew sandbox (no .git dir)
- Fixed `src/cli/commands/init.ts`: added missing imports (`appendFileSync`, `readFileSync`, `homedir`, `join`) and defined `snippet`
- Fixed biome lint errors: `noNonNullAssertion` in `CandidateSelect.tsx` and `client-result.test.ts` (captured narrowed fn in const), `.releaserc.json` format
- Updated `lefthook.yml`: named steps, silent on success, `vitest run --related {staged_files}` for tests, `biome check {staged_files}` for lint
- Updated `biome.json`: added `.gsd` and `.planning` to ignore list
- Updated `package.json` lint scripts: `biome check .` instead of explicit file list
- Updated `README.md`: Homebrew install flow (2 steps, caveats handle `qq init`), API key resolution section (env var + `.env.local` upward search), npm in `<details>`
- **Root bug found and fixed**: `isDirectRun` symlink mismatch — production Homebrew binary never called `main()` — fixed with `fs.realpathSync(process.argv[1])`
</completed_work>

<remaining_work>

**Immediate (next session start):**
1. Commit and push the pending fix:
   ```
   git add src/cli/main.ts README.md .wolf/buglog.json .wolf/memory.md .wolf/hooks/_session.json .wolf/anatomy.md
   git commit -m "fix: resolve symlink in isDirectRun check so Homebrew binary invokes main"
   git push origin main
   ```
2. Wait for CI → semantic-release → homebrew-releaser to update the tap
3. `brew upgrade queque && unset QQ_DEV_ROOT` then test `??` in plain terminal (not Zellij)
4. Investigate Zellij pane named `terminal_XX` instead of `qq` (cosmetic — `--name` flag may need different Zellij version syntax)

**Cleanup (from previous handoff):**
- Pop beta stash: `git checkout beta && git stash pop`
- Delete branch: `git branch -d chore/sync-install-flow-to-main && git push origin --delete chore/sync-install-flow-to-main`

**Discovery / distribution:**
- Record demo gif of `??` trigger (manual — user does this)
- Draft and submit awesome-zsh-plugins PR to `unixorn/awesome-zsh-plugins`
</remaining_work>

<decisions_made>

- `fs.realpathSync(process.argv[1])` is the right fix — resolves symlinks before URL comparison, works for Homebrew cellar paths, npm global installs, and dev runs
- `lefthook.yml` typecheck stays as full `tsc --noEmit` (not staged files) — passing files to tsc ignores tsconfig.json (TS5112)
- `vitest run --related {staged_files}` in pre-commit: uses run mode (not watch), scoped to module graph of staged files; CI runs full suite
- biome.json `files.includes` is the canonical ignore list — not per-script file lists
</decisions_made>

<blockers>
None.
</blockers>

## Infrastructure State
- Local branch: `main`, 1 commit ahead of origin (the isDirectRun fix not yet pushed)
- npm: `@k-leumas/queque-cli` latest: 0.2.12, beta: 0.2.0-beta.X
- Homebrew tap: currently at 0.2.12 (formula with broken `main()` — fix pending release)
- `QQ_DEV_ROOT` should be UNSET when testing Homebrew binary

<context>
The session resolved a chain of Homebrew distribution bugs. The final and most impactful bug was `isDirectRun`: the production binary exits 0 silently because `process.argv[1]` is the symlink path (`/usr/local/bin/qq`) while `import.meta.url` resolves to the Cellar path — they never match. Fix is one line. Zellij reportedly "worked" during the session but may have been a different issue (pane naming); the inline path was definitely broken.
</context>

<next_action>
Commit and push the pending fix: `git add src/cli/main.ts README.md .wolf/buglog.json .wolf/memory.md .wolf/hooks/_session.json .wolf/anatomy.md && git commit -m "fix: resolve symlink in isDirectRun check so Homebrew binary invokes main" && git push origin main`
</next_action>

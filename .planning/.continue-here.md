---
context: default
phase: null
task: null
total_tasks: null
status: paused
last_updated: 2026-05-30T16:46:54.934Z
---

# Session Handoff — Install Flow & Shell Init Fix

No blocking constraints.

<current_state>
Release pipeline working. Homebrew install conflict resolved. `qq init zsh` now outputs a single `source` line instead of inlining 200+ lines into .zshrc. Homebrew formula updated to install the shell script to `share/queque/` for a stable, upgrade-safe path. beta is at v0.2.0-beta.6.
</current_state>

<completed_work>

- Rewrote CHANGELOG.md — removed duplicates, orphaned beta entries, old que-que era content
- Created published GitHub Release for v0.1.0 (was missing from sidebar)
- Deleted 3 orphaned draft releases (v1.0.0 x2, v1.1.0 from old que-que era)
- Diagnosed and fixed Homebrew tap conflict: `homebrew-queque` is a renamed ghost of `homebrew-tap` (same repo, two tap names → `k-leumas/queque` and `k-leumas/tap` both appeared in brew install). Removed formula from the redirect, restored in `homebrew-tap` with jq dependency.
- Fixed README install command: was `brew tap k-leumas/queque && brew install queque-cli` (both wrong). Now: `brew install k-leumas/tap/queque` with a note that it auto-taps.
- Fixed `qq init zsh`: was dumping full 200-line script into .zshrc. Now outputs `source "/path/to/queque.zsh"`.
- Homebrew formula updated: `(share/"queque").install "shell/zsh/queque.zsh"` so the path `$(brew --prefix)/share/queque/queque.zsh` is stable across upgrades.
- `init.ts` updated: checks Homebrew prefix paths first (`/opt/homebrew`, `/usr/local`, `/home/linuxbrew/.linuxbrew`), falls back to `__dirname`-relative for npm global installs.
</completed_work>

<remaining_work>

**Discovery / distribution (do these next):**
- Add npm beta badge and GitHub release badge to README
- Record demo gif of `??` trigger (manual — user does this)
- Draft and submit awesome-zsh-plugins PR to `unixorn/awesome-zsh-plugins`

**Verify Homebrew flow:**
- Confirm `HOMEBREW_TAP_TOKEN` secret is set in the repo (Settings → Secrets → Actions) — the `homebrew.yml` workflow has never successfully run
- The formula was updated directly via API; `homebrew-releaser` will overwrite it on the next release. Ensure `homebrew.yml` `install:` block also includes the `(share/"queque").install` line so the overwrite preserves it
</remaining_work>

<decisions_made>

- `k-leumas/homebrew-tap` (tap: `k-leumas/tap`) is the canonical tap for all tools — `homebrew-queque` was a redirect ghost, not a real separate repo
- `qq init zsh` outputs a `source` line, not inline script — one line in .zshrc, updates automatically when file changes
- Homebrew: shell script installed to `share/queque/` — version-agnostic, survives `brew upgrade`
- `init.ts` detects Homebrew by checking known prefixes via `existsSync`, not by spawning `brew --prefix` — zero runtime cost
- Wolf files always committed via `--amend --no-edit` when they are the only pending change — never a separate commit
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — full structured state
2. `src/cli/commands/init.ts` — Homebrew detection logic, understand before modifying
3. `.github/workflows/homebrew.yml` — needs `(share/"queque").install` added to install block

## Infrastructure State
- GitHub repo: `k-leumas/queque` — public
- npm package: `@k-leumas/queque-cli` — `latest: 0.1.0`, `beta: 0.2.0-beta.6`
- Git tags: `v0.1.0`, `v0.2.0-beta.1`, `v0.2.0-beta.2` (correct commits)
- GitHub Releases: `v0.1.0` (Latest), `v0.2.0-beta.2` (Pre-release)
- Homebrew tap: `k-leumas/homebrew-tap` → accessible as `k-leumas/tap`
- Formula: `Formula/queque.rb` — v0.1.0, has jq, has `(share/"queque").install`, uses npm install method
- `HOMEBREW_TAP_TOKEN` secret: status unknown — workflow has never run

<context>
This session fixed the install flow end-to-end: tap conflict, wrong install command in README, and the jarring `qq init zsh` behavior that inlined the entire shell script. The homebrew path stability issue was the motivating problem — Homebrew Cellar paths include version numbers and go stale on upgrade. The share/ approach fixes this permanently. Next session is quick: badges, demo gif, then the awesome-zsh-plugins PR.
</context>

<next_action>
Start with: Add npm and release badges to README. Then check if HOMEBREW_TAP_TOKEN secret is configured. Then record demo gif. Then submit awesome-zsh-plugins PR.
</next_action>

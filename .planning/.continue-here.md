---
context: default
phase: null
task: null
total_tasks: null
status: paused
last_updated: 2026-05-30T17:25:11.000Z
---

# Session Handoff — Homebrew Install Fix & Main Sync

No blocking constraints.

<current_state>
main pushed at c7a1b56. CI release workflow should be running and will publish v0.2.0 to npm and create a GitHub Release. homebrew-releaser then fires and updates the formula to reference the v0.2.0 tarball (which has the correct shell/zsh/queque.zsh). The brew install ENOENT error will be fixed once that formula lands.
</current_state>

<completed_work>

- Diagnosed brew install failure: v0.1.0 tarball had `shell/zsh/qq.zsh`, formula expected `shell/zsh/queque.zsh` (renamed in beta) — ENOENT
- Fixed `homebrew.yml` install block: added `(share/"queque").install "shell/zsh/queque.zsh"` (was missing)
- Added npm stable/beta/release badges to README
- Cherry-picked all install flow changes from beta to main via direct file apply (avoiding wolf-contaminated commits):
  - `shell/zsh/queque.zsh` (renamed from qq.zsh)
  - `src/cli/commands/init.ts` (new — Homebrew path detection)
  - `src/cli/main.ts` (add init subcommand)
  - `.github/workflows/homebrew.yml` (tap name + jq dep + share install)
  - `.github/workflows/release.yaml` (npm dist-tag fix)
  - `.releaserc.json` (all commit types trigger release + npm plugin)
  - `README.md` (user-facing with correct install + badges)
  - `tsup.config.ts` (shebang banner)
  - `pnpm-lock.yaml` / `package.json` (added @semantic-release/npm)
- Resolved linear history: reset to origin/main, cherry-picked feat commit, resolved README conflict keeping beta version
- Pushed main → CI triggered
</completed_work>

<remaining_work>

**Verify release (do first):**
- Check CI at github.com/k-leumas/queque/actions — confirm semantic-release ran and v0.2.0 published
- Wait for homebrew-releaser to update Formula/queque.rb in k-leumas/homebrew-tap
- Test: `brew upgrade queque` (or fresh install) — should no longer ENOENT

**Cleanup:**
- Pop beta stash: `git checkout beta && git stash pop` (wolf files from before branch switch)
- Delete branch: `git branch -d chore/sync-install-flow-to-main && git push origin --delete chore/sync-install-flow-to-main`

**Discovery / distribution:**
- Record demo gif of `??` trigger (manual — user does this)
- Draft and submit awesome-zsh-plugins PR to `unixorn/awesome-zsh-plugins`
</remaining_work>

<decisions_made>

- Cherry-pick via direct file apply (`git checkout beta -- <file>`), not individual commit cherry-picks — avoids wolf-file bleed from contaminated commits
- Kept beta README over remote main's 7 doc commits — remote had wrong install pattern (`eval qq init zsh` instead of `>>`)
- Version bumped to v0.2.0 (not v0.1.1) — `feat:` commit correctly triggers minor bump; `qq init` is a new command
- Dev vs Homebrew distinction: `QQ_DEV_ROOT` env var toggles which binary the widget uses; unset = Homebrew, set = local node build
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — full structured state
2. `.github/workflows/homebrew.yml` — verify share install line is present
3. `src/cli/commands/init.ts` — Homebrew prefix detection logic

## Infrastructure State
- GitHub repo: `k-leumas/queque` — public, main at `c7a1b56`
- npm: `@k-leumas/queque-cli` — latest: 0.1.0 (v0.2.0 release in progress), beta: 0.2.0-beta.6
- Git tags: `v0.1.0`, `v0.2.0-beta.1` through `v0.2.0-beta.6`
- GitHub Releases: `v0.1.0` (Latest), `v0.2.0-beta.2` (Pre-release) — v0.2.0 pending CI
- Homebrew tap: `k-leumas/homebrew-tap` → `k-leumas/tap`
- `HOMEBREW_TAP_TOKEN` secret: confirmed set
- Local branch `chore/sync-install-flow-to-main`: exists, delete after confirming main is good
- Beta stash: 1 entry (wolf files) — pop when back on beta

<context>
Root cause of brew ENOENT: v0.1.0 tarball has qq.zsh, formula expected queque.zsh (renamed in beta). Rather than patching the formula again manually, brought the full install flow from beta to main and cut a proper v0.2.0 release. homebrew-releaser generates the formula from the v0.2.0 tarball which has the right filename. The release workflow fires automatically on push to main.
</context>

<next_action>
Check CI at github.com/k-leumas/queque/actions — confirm semantic-release triggered and v0.2.0 published. Then wait for homebrew-releaser. Then test brew install/upgrade.
</next_action>

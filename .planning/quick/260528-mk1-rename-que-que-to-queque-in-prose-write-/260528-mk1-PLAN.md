---
phase: quick
plan: 260528-mk1
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - README.md
  - CLAUDE.md
  - docs/SYSTEM_DESGN.md
  - docs/RELEASING.md
  - shell/zsh/qq.zsh
  - src/ui/CandidateSelect.tsx
  - src/providers/claude.ts
  - src/client/run-foreground.ts
  - src/cli/main.ts
  - tests/zsh-widget.test.ts
  - tests/client-result.test.ts
  - tests/shell-contract.test.ts
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/research/ARCHITECTURE.md
  - .planning/research/FEATURES.md
  - .planning/research/PITFALLS.md
  - .planning/research/STACK.md
  - .planning/research/SUMMARY.md
  - .planning/phases/03.1-update-interface-and-interactivity-to-match-that-of-this-git/03.1-UI-SPEC.md
  - .planning/phases/03.1-update-interface-and-interactivity-to-match-that-of-this-git/03.1-PATTERNS.md
  - .planning/phases/04-fuzzy-tui-selection-ux/.continue-here.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "No file in the repo contains 'que-que' or 'Que-Que' (case variants) after the rename"
    - "package.json name field is 'queque'"
    - "All prose references say 'QueQue' (title case)"
    - "The lowercase display label 'que-que ›' in shell/TUI output is updated to 'queque ›'"
    - "The 'qq' binary name is unchanged"
    - "No TypeScript/JS code identifiers or variable names are changed"
    - "All tests still pass after the rename"
  artifacts:
    - path: "package.json"
      provides: "Updated package name field"
      contains: '"name": "queque"'
    - path: "README.md"
      provides: "Updated project name in prose"
  key_links:
    - from: "shell/zsh/qq.zsh"
      to: "que-que › display label (line 133)"
      via: "print -P dimColor line"
      pattern: "queque"
    - from: "src/ui/CandidateSelect.tsx"
      to: "que-que › display label (line 164)"
      via: "Text dimColor"
      pattern: "queque"
---

<objective>
Rename the project from "que-que" (hyphenated) to "queque" (no hyphen) across the entire codebase.

Purpose: Establish a consistent single-word brand name. In prose and docs: "QueQue". In the package.json name field and npm identifier: "queque". In all shell/TUI display labels (lowercase): "queque". The "qq" CLI binary is unchanged.

Output: All occurrences of "que-que" and "Que-Que" replaced; all tests pass; a lint-clean commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename in package.json and all planning/docs markdown files</name>
  <files>
    package.json,
    README.md,
    CLAUDE.md,
    docs/SYSTEM_DESGN.md,
    docs/RELEASING.md,
    .planning/PROJECT.md,
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md,
    .planning/research/ARCHITECTURE.md,
    .planning/research/FEATURES.md,
    .planning/research/PITFALLS.md,
    .planning/research/STACK.md,
    .planning/research/SUMMARY.md,
    .planning/phases/03.1-update-interface-and-interactivity-to-match-that-of-this-git/03.1-UI-SPEC.md,
    .planning/phases/03.1-update-interface-and-interactivity-to-match-that-of-this-git/03.1-PATTERNS.md,
    .planning/phases/04-fuzzy-tui-selection-ux/.continue-here.md
  </files>
  <action>
    Apply these substitutions to each file listed. Use the Edit tool (never sed/awk) to make precise targeted edits.

    Substitution rules (apply in this order within each file):
    1. "Que-Que" (title case hyphenated) → "QueQue"
    2. "que-que" (lowercase hyphenated) → "queque"

    In package.json specifically:
    - Change the "name" field value from "que-que" to "queque"
    - Change the repository URL from "https://github.com/k-leumas/que-que.git" to "https://github.com/k-leumas/queque.git" (update only the package name portion of the URL — do not change the GitHub username or repo path if the actual repo URL is different; update the string literally as it appears)

    In README.md:
    - The env var example `QQ_DEV_ROOT="$HOME/dev/que-que"` should become `QQ_DEV_ROOT="$HOME/dev/queque"` (this is a path example in prose, not a code identifier)

    In the 03.1-UI-SPEC.md and 03.1-PATTERNS.md files:
    - The lowercase UI label "que-que" used as modal title text (a display string, not an identifier) should become "queque"

    Do NOT change:
    - Variable names, function names, or any TypeScript/JS identifiers
    - The "qq" binary or command references
    - The "QQ_" env var prefix names (those are identifiers, not the product name)
    - File names or directory names
  </action>
  <verify>
    Run: grep -rn "que-que\|Que-Que" package.json README.md CLAUDE.md docs/ .planning/PROJECT.md .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/research/ .planning/phases/03.1-update-interface-and-interactivity-to-match-that-of-this-git/ .planning/phases/04-fuzzy-tui-selection-ux/.continue-here.md 2>/dev/null | grep -v ".claude/worktrees" | grep -v "tmp/"

    Expected output: no lines (zero matches).
  </verify>
  <done>Zero "que-que" or "Que-Que" occurrences remain in any of the listed files.</done>
</task>

<task type="auto">
  <name>Task 2: Rename in source code prose strings, shell script, and tests</name>
  <files>
    shell/zsh/qq.zsh,
    src/ui/CandidateSelect.tsx,
    src/providers/claude.ts,
    src/client/run-foreground.ts,
    src/cli/main.ts,
    tests/zsh-widget.test.ts,
    tests/client-result.test.ts,
    tests/shell-contract.test.ts
  </files>
  <action>
    Apply these substitutions to each source file. Use the Edit tool for targeted edits.

    Substitution rules:
    1. String literals and comments containing "Que-Que" → "QueQue"
    2. String literals and comments containing "que-que" → "queque"

    Key locations (read each file to find the exact line before editing):

    shell/zsh/qq.zsh:
    - Comment on line 2: "# qq.zsh — Que-Que ZLE widget..." → "# qq.zsh — QueQue ZLE widget..."
    - Comment near line 130: "#   que-que › <original-query>" → "#   queque › <original-query>"
    - Display string near line 133: print -P dimColor "que-que › ${escaped_query}" → "queque › ${escaped_query}"
    - Comment near line 170: "# Que-Que using the Zellij..." → "# QueQue using the Zellij..."

    src/ui/CandidateSelect.tsx:
    - Display string: `que-que › ${initialQuery}` → `queque › ${initialQuery}`

    src/providers/claude.ts:
    - Error string: 'Que-Que: unexpected response format' → 'QueQue: unexpected response format'
    - System prompt string: 'You are Que-Que, a terminal shell assistant...' → 'You are QueQue, a terminal shell assistant...'

    src/client/run-foreground.ts:
    - Error message strings: `Que-Que: ${message}` → `QueQue: ${message}` (appears twice, ~lines 256 and 272)

    src/cli/main.ts:
    - Error log strings: 'Que-Que: uncaught exception:' → 'QueQue: uncaught exception:' and 'Que-Que: unhandled rejection:' → 'QueQue: unhandled rejection:'

    tests/zsh-widget.test.ts:
    - All string literals and comments referencing "que-que" or "Que-Que" → "queque" or "QueQue" respectively
    - The .toContain('que-que › list changed files') assertion → .toContain('queque › list changed files')
    - The .toContain('que-que') assertions → .toContain('queque')
    - The JSON message string 'Que-Que: API timeout...' → 'QueQue: API timeout...'
    - The 'Que-Que requires Zellij' grep pattern → 'QueQue requires Zellij'
    - Descriptive test strings (it/describe strings) containing "que-que" → "queque"

    tests/client-result.test.ts:
    - expect(...).toContain('Que-Que:') → .toContain('QueQue:')

    tests/shell-contract.test.ts:
    - message string: 'Que-Que: API timeout — press any key' → 'QueQue: API timeout — press any key' (appears twice)

    Do NOT change:
    - Variable names, function names, TypeScript/JS identifiers (e.g., qqResult, QQ_*)
    - File paths or directory references
    - The "qq" binary command name
  </action>
  <verify>
    1. Check no occurrences remain:
       grep -rn "que-que\|Que-Que" shell/zsh/qq.zsh src/ tests/ 2>/dev/null | grep -v ".claude/worktrees" | grep -v "tmp/"

       Expected output: no lines.

    2. Run the test suite:
       cd /Users/samuel/dev/tui-llm && pnpm test 2>&1 | tail -20

       Expected: all tests pass (no new failures from the rename).

    3. Run the linter on changed source files:
       cd /Users/samuel/dev/tui-llm && pnpm biome check src/ tests/ 2>&1 | tail -10

       Expected: no errors.
  </verify>
  <done>
    Zero "que-que"/"Que-Que" occurrences in source/test files. Test suite passes. Linter clean.
    The string "queque" (lowercase) appears as the display label in shell/TUI output.
    The string "QueQue" appears in all error messages, system prompts, and prose comments.
  </done>
</task>

<task type="auto">
  <name>Task 3: Final sweep, wolf memory update, and commit</name>
  <files>
    .wolf/anatomy.md,
    .wolf/memory.md
  </files>
  <action>
    1. Run a final broad sweep to confirm no residual "que-que" or "Que-Que" occurrences remain outside worktrees and tmp dirs:
       grep -rn --include="*.md" --include="*.json" --include="*.ts" --include="*.tsx" --include="*.zsh" --include="*.yaml" --include="*.mjs" "que-que\|Que-Que" . 2>/dev/null | grep -v "\.claude/worktrees" | grep -v "tmp/"

       If any hits appear, fix them before committing.

    2. Update .wolf/anatomy.md: change the description for package.json from 'Node.js package manifest' to 'Node.js package manifest (name: queque)' to reflect the rename.

    3. Append a memory entry to .wolf/memory.md:
       | HH:MM | Renamed project from que-que to queque everywhere (prose: QueQue, package name: queque, display label: queque) | package.json, README.md, CLAUDE.md, src/, tests/, shell/, docs/, .planning/ | Done | ~3000 |

    4. Run biome lint on modified source files, fix any issues.

    5. Stage and commit the rename:
       - Stage specific files (do NOT use git add -A):
         git add package.json README.md CLAUDE.md docs/ shell/ src/ tests/ .planning/ .wolf/anatomy.md .wolf/memory.md
       - Commit with message: "chore: rename project from que-que to queque"
         (commitlint enforces conventional commit format; keep it under the project's max length)
  </action>
  <verify>
    git show --stat HEAD | head -30

    Expected: commit appears at HEAD with the expected files changed. No "que-que" in the diff.

    grep -c "que-que\|Que-Que" $(git show --name-only HEAD | tail -n +2 | head -30) 2>/dev/null | grep -v ":0" | head

    Expected: no lines output (all counts are 0).
  </verify>
  <done>
    Commit exists at HEAD. No residual "que-que" or "Que-Que" anywhere in the working tree (outside worktrees/tmp). Wolf memory updated.
  </done>
</task>

</tasks>

<verification>
Run the following after all tasks complete:

1. Full repo scan:
   grep -rn "que-que\|Que-Que" /Users/samuel/dev/tui-llm --include="*.md" --include="*.json" --include="*.ts" --include="*.tsx" --include="*.zsh" --include="*.yaml" 2>/dev/null | grep -v "\.claude/worktrees" | grep -v "tmp/"
   Expected: zero matches.

2. Package name check:
   node -e "const p=require('./package.json'); console.log(p.name)"
   Expected: queque

3. Tests pass:
   pnpm test
   Expected: all green.
</verification>

<success_criteria>
- package.json "name" is "queque"
- All prose occurrences of "Que-Que" are now "QueQue"
- All lowercase prose/display occurrences of "que-que" are now "queque"
- The "qq" binary is unchanged
- No TypeScript/JS identifiers were changed
- pnpm test passes
- A single clean commit at HEAD captures all changes
</success_criteria>

<output>
Create `.planning/quick/260528-mk1-rename-que-que-to-queque-in-prose-write-/260528-mk1-SUMMARY.md` when done.
</output>

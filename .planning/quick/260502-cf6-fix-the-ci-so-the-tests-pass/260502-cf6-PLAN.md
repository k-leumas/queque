# Quick Task 260502-cf6: fix the ci so the tests pass

**Mode:** quick
**Status:** Planned

## Task

Make the release CI test job pass by fixing the shell test harness so noninteractive `zsh` sourcing does not fail during `pnpm test:run`.

## Plan

1. Update `shell/zsh/qq.zsh` so ZLE registration only happens in interactive shells.
   - Files: `shell/zsh/qq.zsh`
   - Action: Guard `zle -N` and `bindkey` behind `[[ -o interactive ]]`.
   - Verify: `zsh -c 'source shell/zsh/qq.zsh; echo ok'` exits 0.
   - Done: The widget still registers in interactive shells and sources cleanly in CI.

2. Verify the widget suite still passes.
   - Files: `tests/zsh-widget.test.ts`
   - Action: Run the widget tests against the updated shell file.
   - Verify: `pnpm test:run tests/zsh-widget.test.ts` passes.
   - Done: CI-sensitive shell behavior is fixed without regressing the widget tests.

---
phase: 01-shell-bridge-and-result-contract
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - shell/zsh/qq.zsh
  - tests/zsh-widget.test.ts
autonomous: true
requirements:
  - SHL-01
  - SHL-02
  - SHL-03
  - SHL-04
must_haves:
  truths:
    - "Typing a single `?` still inserts immediately without a visible KEYTIMEOUT pause."
    - "Typing `??` captures the pre-trigger shell buffers and invokes Que-Que."
    - "Cancel restores the original shell buffers exactly."
    - "Accepted results write `lbuffer` and `rbuffer` back into the live shell line."
  artifacts:
    - path: "shell/zsh/qq.zsh"
      provides: "ZLE widget, launch helper, and shell-side result application"
      contains: "zle -N"
    - path: "tests/zsh-widget.test.ts"
      provides: "Spawned-zsh smoke coverage for trigger and cancel behavior"
      contains: "??"
  key_links:
    - from: "shell/zsh/qq.zsh"
      to: "qq client --request-file --result-file"
      via: "TTY-attached subprocess launch"
      pattern: "/dev/tty"
    - from: "shell/zsh/qq.zsh"
      to: "ShellResult"
      via: "JSON result parsing and LBUFFER/RBUFFER assignment"
      pattern: "replace-buffer|cancel"
---

<objective>
Implement the real `zsh` bridge: intercept `??` through a `?` widget, preserve shell state on cancel, and apply split-buffer results on accept.

Purpose: This is the user-visible shell seam that makes Que-Que feel native instead of acting like a separate prompt-taking CLI.
Output: A sourceable `zsh` integration script and a smoke test that locks the trigger/cancel/apply behavior.
</objective>

<execution_context>
@/Users/samuel/.codex/get-shit-done/workflows/execute-plan.md
@/Users/samuel/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-shell-bridge-and-result-contract/01-RESEARCH.md
@.planning/phases/01-shell-bridge-and-result-contract/01-01-PLAN.md
@CLAUDE.md

<interfaces>
Consume the Plan 01 shell contract exactly:

```ts
type ShellRequest = {
  version: 1;
  ttyPath: string;
  cwd: string;
  shellPid: number;
  lbuffer: string;
  rbuffer: string;
};

type ShellResult =
  | {kind: 'cancel'}
  | {kind: 'replace-buffer'; lbuffer: string; rbuffer: string};
```

The shell launcher must call:

```bash
qq client --request-file "$request_file" --result-file "$result_file"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the `?` ZLE widget and request capture path</name>
  <files>shell/zsh/qq.zsh, tests/zsh-widget.test.ts</files>
  <read_first>.planning/phases/01-shell-bridge-and-result-contract/01-RESEARCH.md, .planning/phases/01-shell-bridge-and-result-contract/01-01-PLAN.md, shell/zsh/qq.zsh, tests/zsh-widget.test.ts, src/contracts/shell.ts, src/shared/socket-path.ts</read_first>
  <behavior>
    - Test 1: typing a single `?` leaves the line unchanged except for the inserted `?`.
    - Test 2: typing `??` removes the trigger from the request payload and records the original `lbuffer` and `rbuffer`.
    - Test 3: the client subprocess launch uses `</dev/tty >/dev/tty 2>&1`.
  </behavior>
  <action>Create `shell/zsh/qq.zsh` with a custom `?` ZLE widget that calls `.self-insert` for the first `?`, then detects a trailing `?` in `LBUFFER` to consume the literal `??` trigger without introducing a `KEYTIMEOUT` delay for a standalone `?`. Save the original `LBUFFER` and `RBUFFER`, build a request JSON object containing `version`, `ttyPath`, `cwd`, `shellPid`, `lbuffer`, and `rbuffer`, and invoke `qq client --request-file "$request_file" --result-file "$result_file"` with stdin/stdout explicitly reattached using `</dev/tty >/dev/tty 2>&1`. Write `tests/zsh-widget.test.ts` first as a spawned-`zsh` smoke harness that exercises single `?`, `??`, and launch command assembly, then implement the widget until it passes.</action>
  <verify>
    <automated>pnpm vitest run tests/zsh-widget.test.ts -x</automated>
  </verify>
  <acceptance_criteria>`pnpm vitest run tests/zsh-widget.test.ts -x` exits 0; `grep -q "zle -N" shell/zsh/qq.zsh` succeeds; `grep -q "/dev/tty" shell/zsh/qq.zsh` succeeds; `grep -q "qq client --request-file" shell/zsh/qq.zsh` succeeds; `grep -q "LBUFFER" shell/zsh/qq.zsh` and `grep -q "RBUFFER" shell/zsh/qq.zsh` both succeed.</acceptance_criteria>
  <done>The shell integration intercepts `??` through a custom `?` widget, captures split buffers without the trigger, and launches the foreground client with a live TTY.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply cancel and replace-buffer results safely in the live shell</name>
  <files>shell/zsh/qq.zsh, tests/zsh-widget.test.ts</files>
  <read_first>.planning/phases/01-shell-bridge-and-result-contract/01-RESEARCH.md, shell/zsh/qq.zsh, tests/zsh-widget.test.ts, src/contracts/shell.ts</read_first>
  <behavior>
    - Test 1: `{kind: 'cancel'}` restores the exact saved `LBUFFER` and `RBUFFER`.
    - Test 2: `{kind: 'replace-buffer', lbuffer: 'git status', rbuffer: ''}` writes the new buffers back to the shell.
    - Test 3: malformed result JSON leaves the original buffers intact and returns a nonzero shell function status.
  </behavior>
  <action>Extend `shell/zsh/qq.zsh` with a shell-side result reader that parses the JSON result file, branches only on `kind: 'cancel'` and `kind: 'replace-buffer'`, restores the saved original buffers for cancel, and assigns `LBUFFER` plus `RBUFFER` directly for accepted results. Do not compute or store a numeric cursor index anywhere in the shell bridge. For malformed JSON or unsupported kinds, keep the original buffers untouched and return failure so later debugging is possible. Add test coverage for cancel, replace-buffer, and malformed-result cases in `tests/zsh-widget.test.ts`.</action>
  <verify>
    <automated>pnpm vitest run tests/zsh-widget.test.ts -x</automated>
  </verify>
  <acceptance_criteria>`pnpm vitest run tests/zsh-widget.test.ts -x` exits 0; `grep -q "replace-buffer" shell/zsh/qq.zsh` succeeds; `grep -q "cancel" shell/zsh/qq.zsh` succeeds; `rg -n "cursor" shell/zsh/qq.zsh` returns no matches; `rg -n "malformed|invalid" tests/zsh-widget.test.ts` shows a malformed-result case.</acceptance_criteria>
  <done>The shell bridge preserves user state on cancel, applies accepted split-buffer replacements directly, and rejects malformed results without mutating the command line.</done>
</task>

</tasks>

<verification>
Run `pnpm vitest run tests/zsh-widget.test.ts -x`. In a manual smoke shell, source `shell/zsh/qq.zsh`, type a single `?`, then type `??` after some existing text and verify cancel leaves the line unchanged.
</verification>

<success_criteria>
The `zsh` bridge honors the Phase 1 shell UX: literal `??` opens Que-Que from the live edit session, cancel is lossless, and accepted results come back through `{kind,lbuffer,rbuffer}` only.
</success_criteria>

<output>
After completion, create `.planning/phases/01-shell-bridge-and-result-contract/01-02-SUMMARY.md`
</output>

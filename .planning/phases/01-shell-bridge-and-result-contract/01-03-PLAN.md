---
phase: 01-shell-bridge-and-result-contract
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/cli/main.ts
  - src/cli/commands/client.ts
  - src/cli/commands/daemon.ts
  - src/client/result-writer.ts
  - src/client/run-foreground.ts
  - src/daemon/bootstrap.ts
  - src/daemon/server.ts
  - tests/client-result.test.ts
  - tests/daemon-bootstrap.test.ts
autonomous: true
requirements:
  - SHL-03
  - SHL-04
  - RUN-01
must_haves:
  truths:
    - "A foreground client can ensure the daemon is available without owning the TTY."
    - "The daemon listens on a short per-user Unix socket path and recovers from stale sockets."
    - "The foreground client can deterministically emit either cancel or replace-buffer results for the shell bridge to apply."
    - "The top-level CLI entrypoint dispatches `qq client` and `qq daemon` to the real command handlers instead of placeholder bodies."
  artifacts:
    - path: "src/daemon/bootstrap.ts"
      provides: "Auto-bootstrap and reconnect logic"
      exports: ["ensureDaemon"]
    - path: "src/daemon/server.ts"
      provides: "Unix-socket server on /tmp/qq-$UID.sock"
      exports: ["startDaemonServer"]
    - path: "src/client/run-foreground.ts"
      provides: "TTY-attached foreground loop"
      exports: ["runForegroundClient"]
    - path: "src/client/result-writer.ts"
      provides: "Shell result file emission"
      exports: ["writeShellResult"]
    - path: "src/cli/main.ts"
      provides: "Top-level command registration wired to the real handlers"
      exports: ["main"]
  key_links:
    - from: "src/cli/main.ts"
      to: "src/cli/commands/client.ts"
      via: "command registration"
      pattern: "client"
    - from: "src/cli/main.ts"
      to: "src/cli/commands/daemon.ts"
      via: "command registration"
      pattern: "daemon"
    - from: "src/cli/commands/client.ts"
      to: "src/daemon/bootstrap.ts"
      via: "ensure before query submission"
      pattern: "ensureDaemon"
    - from: "src/client/run-foreground.ts"
      to: "src/client/result-writer.ts"
      via: "deterministic cancel and replace-buffer result emission"
      pattern: "writeShellResult"
    - from: "src/daemon/server.ts"
      to: "src/shared/socket-path.ts"
      via: "listen path selection"
      pattern: "/tmp/qq-"
---

<objective>
Stand up the daemon bootstrap path and a minimal foreground client loop behind the CLI surface defined in Plan 01.

Purpose: Repeat invocations need a warm background process, but the live TTY must remain owned by the foreground client so `Esc` and later TUI input work correctly.
Output: Daemon bootstrap/server modules, foreground client/result writer modules, and focused tests for bootstrap and shell-result emission.
</objective>

<execution_context>
@/Users/samuel/.codex/get-shit-done/workflows/execute-plan.md
@/Users/samuel/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-shell-bridge-and-result-contract/01-RESEARCH.md
@.planning/phases/01-shell-bridge-and-result-contract/01-01-PLAN.md
@CLAUDE.md

<interfaces>
Consume the Plan 01 contracts and keep them UI-neutral:

```ts
type ShellResult =
  | {kind: 'cancel'}
  | {kind: 'replace-buffer'; lbuffer: string; rbuffer: string};

declare function ensureDaemon(socketPath: string): Promise<void>;
declare function startDaemonServer(socketPath: string): Promise<void>;
declare function runForegroundClient(args: {
  requestFile: string;
  resultFile: string;
  resultMode: 'cancel' | 'replace-buffer-fixture';
}): Promise<void>;
declare function writeShellResult(resultFile: string, result: ShellResult): Promise<void>;
```

The client must attach to `/dev/tty` for interactive use and the daemon must never depend on Ink or React imports.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build daemon bootstrap and wire the daemon command through the real CLI entrypoint</name>
  <files>src/cli/main.ts, src/cli/commands/daemon.ts, src/daemon/bootstrap.ts, src/daemon/server.ts, tests/daemon-bootstrap.test.ts</files>
  <read_first>.planning/phases/01-shell-bridge-and-result-contract/01-RESEARCH.md, .planning/phases/01-shell-bridge-and-result-contract/01-01-PLAN.md, src/cli/main.ts, src/cli/commands/daemon.ts, src/daemon/bootstrap.ts, src/daemon/server.ts, tests/daemon-bootstrap.test.ts, src/contracts/ipc.ts, src/shared/socket-path.ts</read_first>
  <behavior>
    - Test 1: `ensureDaemon('/tmp/qq-501.sock')` starts the daemon when no socket exists.
    - Test 2: bootstrap unlinks a stale socket file after a failed connect and then relistens.
    - Test 3: the server responds to `{kind: 'ping'}` with `{kind: 'pong'}`.
  </behavior>
  <action>Create `src/daemon/server.ts` using Node `net` to listen on the socket path from `src/shared/socket-path.ts` and handle at least `ping` plus `ensure-session` requests from `src/contracts/ipc.ts`. Create `src/daemon/bootstrap.ts` with an exported `ensureDaemon` helper that tries to connect first, unlinks stale sockets on connect failure, and spawns `qq daemon --socket <path>` as a detached child when needed. Create `src/cli/commands/daemon.ts` to expose a real daemon command handler, then update `src/cli/main.ts` so the `daemon` subcommand and `--ensure` flow dispatch to that handler instead of keeping the Phase 1 placeholder body. Write `tests/daemon-bootstrap.test.ts` first to cover missing-socket startup, stale-socket recovery, and a `ping` roundtrip, then implement until the test passes. Keep the daemon free of Ink, React, and any `/dev/tty` assumptions.</action>
  <verify>
    <automated>pnpm vitest run tests/daemon-bootstrap.test.ts -x</automated>
  </verify>
  <acceptance_criteria>`pnpm vitest run tests/daemon-bootstrap.test.ts -x` exits 0; `grep -q "ensureDaemon" src/daemon/bootstrap.ts` succeeds; `grep -q "createServer" src/daemon/server.ts` succeeds; `grep -q "unlink" src/daemon/bootstrap.ts` succeeds; `grep -q "from './commands/daemon'" src/cli/main.ts` succeeds; `rg -n "ink|react" src/daemon src/cli/commands/daemon.ts` returns no matches.</acceptance_criteria>
  <done>The daemon can bootstrap itself on a short socket path, recover from stale sockets, answer basic IPC health checks, and the top-level CLI now routes `qq daemon` to the real handler.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the foreground client loop, deterministic accepted-result seam, and client CLI wiring</name>
  <files>src/cli/main.ts, src/cli/commands/client.ts, src/client/result-writer.ts, src/client/run-foreground.ts, tests/client-result.test.ts</files>
  <read_first>.planning/phases/01-shell-bridge-and-result-contract/01-RESEARCH.md, .planning/phases/01-shell-bridge-and-result-contract/01-01-PLAN.md, src/cli/main.ts, src/cli/commands/client.ts, src/client/result-writer.ts, src/client/run-foreground.ts, tests/client-result.test.ts, src/contracts/shell.ts, src/daemon/bootstrap.ts</read_first>
  <behavior>
    - Test 1: `writeShellResult` writes `{kind: 'cancel'}` JSON exactly once to the result file.
    - Test 2: `writeShellResult` writes `{kind: 'replace-buffer', lbuffer, rbuffer}` JSON exactly once to the result file.
    - Test 3: `runForegroundClient` reads the request file, calls `ensureDaemon`, and emits a deterministic `replace-buffer` fixture result when invoked in accept mode.
    - Test 4: the foreground client opens `/dev/tty` for stdin/stdout instead of assuming inherited stdio is interactive.
  </behavior>
  <action>Create `src/client/result-writer.ts` with an exported `writeShellResult` helper that validates `ShellResult` and writes newline-terminated JSON to the result file. Create `src/client/run-foreground.ts` with an exported `runForegroundClient` function that reads the request file, validates it with `shellRequestSchema`, calls `ensureDaemon(socketPathForUid(process.getuid()))`, opens `/dev/tty` for the interactive stdio handles, and supports a deterministic Phase 1 seam such as `resultMode: 'cancel' | 'replace-buffer-fixture'`. In `replace-buffer-fixture` mode, emit a real accepted result shaped exactly as `{kind: 'replace-buffer', lbuffer, rbuffer}` so the shell bridge can round-trip an accepted selection without needing the later Ink UX; the fixture can be static or derived from the request, but it must be deterministic and testable. In `cancel` mode, still emit `{kind: 'cancel'}`. Create `src/cli/commands/client.ts` to expose a real client handler and update `src/cli/main.ts` so `qq client --request-file --result-file` dispatches to it, with an explicit CLI flag or environment seam selecting the deterministic result mode for testing and manual smoke verification. Keep this module free of Ink imports for now; only establish the TTY, daemon bootstrap, and result-writing seam so Phase 4 can add UI later without changing the shell contract.</action>
  <verify>
    <automated>pnpm vitest run tests/client-result.test.ts -x</automated>
  </verify>
  <acceptance_criteria>`pnpm vitest run tests/client-result.test.ts -x` exits 0; `grep -q "/dev/tty" src/client/run-foreground.ts` succeeds; `grep -q "writeShellResult" src/client/run-foreground.ts` succeeds; `grep -q "replace-buffer" src/client/run-foreground.ts` succeeds; `grep -q "from './commands/client'" src/cli/main.ts` succeeds; `rg -n "ink|react" src/client src/cli/commands/client.ts` returns no matches.</acceptance_criteria>
  <done>The CLI has a real foreground client path that validates shell requests, ensures the daemon is running, attaches to `/dev/tty`, and can deterministically emit either cancel or accepted split-buffer results for the shell bridge to consume.</done>
</task>

</tasks>

<verification>
Run `pnpm vitest run tests/daemon-bootstrap.test.ts tests/client-result.test.ts -x` and `pnpm typecheck`. Then run a manual smoke through the real CLI seam by sourcing `shell/zsh/qq.zsh`, triggering `??`, and selecting the deterministic accept mode exposed by Task 2 so the client writes a real `{kind:'replace-buffer', lbuffer, rbuffer}` result that the shell applies. Confirm the daemon path stays under `/tmp/qq-$UID.sock`, `qq client` and `qq daemon` both route through `src/cli/main.ts`, and the foreground client writes structured shell results without importing Ink.
</verification>

<success_criteria>
Phase 1 has a working bootstrap seam for a warm daemon, a foreground client seam that owns the TTY, and a deterministic accepted-result round trip through the real CLI wiring, without leaking UI framework details into shell or IPC contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/01-shell-bridge-and-result-contract/01-03-SUMMARY.md`
</output>

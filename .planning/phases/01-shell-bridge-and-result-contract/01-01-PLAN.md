---
phase: 01-shell-bridge-and-result-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .nvmrc
  - package.json
  - tsconfig.json
  - tsup.config.ts
  - vitest.config.ts
  - src/cli/main.ts
  - src/contracts/ipc.ts
  - src/contracts/shell.ts
  - src/shared/socket-path.ts
  - tests/shell-contract.test.ts
autonomous: true
requirements:
  - SHL-02
  - SHL-04
must_haves:
  truths:
    - "The repo installs and builds with the locked Node LTS and pnpm toolchain."
    - "Shell and daemon payloads are defined in UI-neutral zod schemas."
    - "The CLI exposes stable qq subcommands for later shell and daemon plans."
  artifacts:
    - path: ".nvmrc"
      provides: "Pinned Node LTS version"
      contains: "24."
    - path: "package.json"
      provides: "pnpm scripts and runtime dependencies"
      contains: "\"build\""
    - path: "src/contracts/shell.ts"
      provides: "Shell request/result schemas using {kind,lbuffer,rbuffer}"
      exports: ["shellRequestSchema", "shellResultSchema"]
    - path: "src/contracts/ipc.ts"
      provides: "Daemon request/result schemas"
      exports: ["ipcRequestSchema", "ipcResponseSchema"]
    - path: "src/cli/main.ts"
      provides: "qq CLI skeleton with client and daemon subcommands"
      exports: ["main"]
  key_links:
    - from: "package.json"
      to: "src/cli/main.ts"
      via: "bin and build scripts"
      pattern: "qq"
    - from: "src/contracts/shell.ts"
      to: "tests/shell-contract.test.ts"
      via: "schema parsing assertions"
      pattern: "shellResultSchema|shellRequestSchema"
---

<objective>
Establish the repo baseline and the shared contracts that every later Phase 1 plan depends on.

Purpose: Lock the Node/pnpm toolchain, CLI surface, and UI-neutral payload schemas before shell or daemon work starts.
Output: Installable project scaffold, `qq` CLI skeleton, shell/IPC zod schemas, and contract tests.
</objective>

<execution_context>
@/Users/samuel/.codex/get-shit-done/workflows/execute-plan.md
@/Users/samuel/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-shell-bridge-and-result-contract/01-RESEARCH.md
@CLAUDE.md

<interfaces>
Create these contracts in this plan so downstream plans can implement against them without redesigning them:

From `src/contracts/shell.ts`:
```ts
type ShellRequest = {
  version: 1;
  ttyPath: string;
  cwd: string;
  shellPid: number;
  lbuffer: string;
  rbuffer: string;
};

type ShellResult =
  | {kind: 'cancel'}
  | {kind: 'replace-buffer'; lbuffer: string; rbuffer: string};
```

From `src/contracts/ipc.ts`:
```ts
type IpcRequest =
  | {kind: 'ping'}
  | {kind: 'ensure-session'; shellPid: number; ttyPath: string}
  | {kind: 'run-query'; request: ShellRequest};

type IpcResponse =
  | {kind: 'pong'}
  | {kind: 'session-ready'; socketPath: string}
  | {kind: 'query-accepted'; requestId: string};
```

From `src/cli/main.ts`:
```ts
qq client --request-file <path> --result-file <path>
qq daemon --socket <path>
qq daemon --ensure --socket <path>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the toolchain baseline and CLI entry skeleton</name>
  <files>.nvmrc, package.json, tsconfig.json, tsup.config.ts, vitest.config.ts, src/cli/main.ts</files>
  <read_first>.planning/phases/01-shell-bridge-and-result-contract/01-RESEARCH.md, CLAUDE.md, .wolf/cerebrum.md, .nvmrc, package.json, tsconfig.json, tsup.config.ts, vitest.config.ts, src/cli/main.ts</read_first>
  <action>Create a single-package `pnpm` TypeScript CLI baseline pinned to Node `24.14.1` in `.nvmrc`. In `package.json`, define the `qq` bin pointing at `dist/cli/main.js`; add scripts `build`, `dev`, `test`, `test:run`, and `typecheck`; add runtime deps `cac`, `zod`; add dev deps `typescript`, `tsup`, `vitest`, and `@types/node`. Create `tsconfig.json` for NodeNext TypeScript output. Create `tsup.config.ts` that bundles the CLI entry from `src/cli/main.ts` into `dist/cli`. Create `vitest.config.ts` with a Node test environment and `tests/**/*.test.ts` inclusion. In `src/cli/main.ts`, export a `main` function and register the stable subcommands `client`, `daemon`, and `daemon --ensure`, even if the command bodies initially throw explicit `not implemented` errors. Keep the command names and flag names exactly as defined in the interfaces block so later plans can build against them.</action>
  <verify>
    <automated>test -f .nvmrc && grep -q '^24\\.14\\.1$' .nvmrc && grep -q '"build"' package.json && grep -q '"test:run"' package.json && grep -q "client" src/cli/main.ts && grep -q "daemon" src/cli/main.ts</automated>
  </verify>
  <acceptance_criteria>`grep -q '^24\\.14\\.1$' .nvmrc` succeeds; `grep -q '"bin"' package.json` succeeds; `grep -q 'dist/cli/main.js' package.json` succeeds; `grep -q "command('client')" src/cli/main.ts` succeeds; `grep -q "command('daemon')" src/cli/main.ts` succeeds.</acceptance_criteria>
  <done>The repo has a pinned Node version, `pnpm` scripts, a build config, a Vitest config, and a stable `qq` CLI surface that later plans can implement without renaming commands or flags.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define UI-neutral shell and IPC contracts with tests</name>
  <files>src/contracts/shell.ts, src/contracts/ipc.ts, src/shared/socket-path.ts, tests/shell-contract.test.ts</files>
  <read_first>.planning/phases/01-shell-bridge-and-result-contract/01-RESEARCH.md, .planning/REQUIREMENTS.md, src/contracts/shell.ts, src/contracts/ipc.ts, src/shared/socket-path.ts, tests/shell-contract.test.ts</read_first>
  <behavior>
    - Test 1: `shellResultSchema` accepts `{kind: 'cancel'}` and rejects buffer fields on cancel.
    - Test 2: `shellResultSchema` accepts `{kind: 'replace-buffer', lbuffer: 'echo hi', rbuffer: ''}` and rejects any `{buffer, cursor}` shape.
    - Test 3: `shellRequestSchema` preserves separate `lbuffer` and `rbuffer` fields.
    - Test 4: `socketPathForUid(501)` returns `/tmp/qq-501.sock`.
  </behavior>
  <action>Create `src/contracts/shell.ts` with zod schemas and exported TypeScript types for `ShellRequest` and `ShellResult`. The result schema must be a discriminated union with `kind: 'cancel'` and `kind: 'replace-buffer'`, and it must prefer `{kind, lbuffer, rbuffer}` over any `{buffer, cursor}` shape. Create `src/contracts/ipc.ts` with zod schemas for `ping`, `ensure-session`, and `run-query`, plus matching response schemas for `pong`, `session-ready`, and `query-accepted`. Create `src/shared/socket-path.ts` with a helper that always returns `/tmp/qq-$UID.sock` and never reads macOS `TMPDIR`. Create `tests/shell-contract.test.ts` first, make it assert the behavior block, then implement the schemas until the tests pass.</action>
  <verify>
    <automated>pnpm vitest run tests/shell-contract.test.ts -x</automated>
  </verify>
  <acceptance_criteria>`pnpm vitest run tests/shell-contract.test.ts -x` exits 0; `grep -q "replace-buffer" src/contracts/shell.ts` succeeds; `grep -q "lbuffer" src/contracts/shell.ts` succeeds; `grep -q "/tmp/qq-" src/shared/socket-path.ts` succeeds; `rg -n "buffer|cursor" tests/shell-contract.test.ts` shows a rejection case for the deprecated shape.</acceptance_criteria>
  <done>Phase 1 has shared request/result schemas, a fixed short socket-path helper, and passing tests that lock the shell contract before shell or daemon implementations land.</done>
</task>

</tasks>

<verification>
Run `pnpm vitest run tests/shell-contract.test.ts -x` and `pnpm typecheck`. Confirm the generated CLI contract still exposes `client` and `daemon` commands and that shell results are split-buffer only.
</verification>

<success_criteria>
The repo can install against the pinned Node version, the CLI surface is defined, and downstream plans can rely on stable zod contracts for shell and IPC payloads without leaking Ink into those schemas.
</success_criteria>

<output>
After completion, create `.planning/phases/01-shell-bridge-and-result-contract/01-01-SUMMARY.md`
</output>

---
phase: 03.1
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - vitest.config.ts
  - tests/client-result.test.ts
  - tests/candidate-select.test.tsx
autonomous: true
requirements:
  - TUI-01
  - CMD-03
  - SAFE-01

must_haves:
  truths:
    - "pnpm test:run exits 0 after Wave 0 changes (no failing pre-existing assertions)"
    - "tests/candidate-select.test.tsx is discovered by vitest and runs"
    - "The raw ANSI assertion 'QueQue is thinking...' is gone from client-result.test.ts"
    - "The single-candidate fast-accept test is updated to reflect modal-always behavior"
  artifacts:
    - path: "tests/candidate-select.test.tsx"
      provides: "Unit tests for CandidateSelect loading/candidate/filter/nav states"
      min_lines: 60
    - path: "vitest.config.ts"
      provides: "Extended include pattern that discovers .test.tsx files"
      contains: "test.tsx"
    - path: "tests/client-result.test.ts"
      provides: "Updated loading feedback test + updated single-candidate test"
  key_links:
    - from: "vitest.config.ts"
      to: "tests/candidate-select.test.tsx"
      via: "include pattern"
      pattern: "test\\.tsx"
    - from: "tests/client-result.test.ts"
      to: "src/client/run-foreground.ts"
      via: "import and mock of fetchCandidates"
      pattern: "fetchCandidates"
---

<objective>
Wave 0: Establish test infrastructure for Phase 03.1.

Three gaps block the phase from being verifiable:
1. vitest.config.ts only matches `.test.ts` — a new `.test.tsx` file won't be discovered.
2. `tests/client-result.test.ts` line 299 asserts `"QueQue is thinking..."` which D-05 removes.
3. `tests/client-result.test.ts` has a single-candidate test that assumes the fast-accept bypass (D-03 removes it).

This plan closes all three gaps before any implementation work begins, so subsequent waves have green baselines.

Purpose: Ensure pnpm test:run is green before Wave 1 touches any source files. Prevents "is this my change or a pre-existing failure?" ambiguity during execution.
Output: vitest.config.ts (updated), tests/client-result.test.ts (updated), tests/candidate-select.test.tsx (new scaffold with failing tests for Wave 2 to make green).
</objective>

<execution_context>
@/Users/samuel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/samuel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03.1-update-interface-and-interactivity-to-match-that-of-this-git/03.1-CONTEXT.md
@.planning/phases/03.1-update-interface-and-interactivity-to-match-that-of-this-git/03.1-VALIDATION.md
@.planning/phases/03.1-update-interface-and-interactivity-to-match-that-of-this-git/03.1-RESEARCH.md
</context>

<interfaces>
<!-- Key types used in the new test file. Extracted from src/contracts/candidates.ts. -->

From src/contracts/candidates.ts (inferred from usage in CandidateSelect.tsx):
```typescript
// CandidateList is an array of CommandCandidate with .command and .explanation strings
export type CommandCandidate = { command: string; explanation: string };
export type CandidateList = CommandCandidate[];
```

From src/ui/CandidateSelect.tsx (current Props interface — Wave 2 will change this):
```typescript
interface Props {
  candidates: CandidateList;   // Wave 2 makes this CandidateList | null
  onSelect: (command: string) => void;
  onCancel: () => void;
}
```

The new test file must import against the Wave-2 signature (candidates: CandidateList | null).
Write the test file targeting that future signature so tests are RED until Wave 2 implements it.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend vitest.config.ts to discover .test.tsx files</name>
  <files>vitest.config.ts</files>
  <read_first>vitest.config.ts</read_first>
  <action>
    Update the `test.include` array to add `tests/**/*.test.tsx` alongside the existing `tests/**/*.test.ts` entry. The updated array must be:
    `include: ['tests/**/*.test.ts', 'tests/**/*.test.tsx']`.
    No other changes. The environment stays `node`.
  </action>
  <verify>
    <automated>grep -c 'test\.tsx' vitest.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - vitest.config.ts contains the string `test.tsx` in its include array
    - `grep -c 'test\.tsx' vitest.config.ts` returns 1
    - `pnpm test:run` exits 0 (no regressions from this change alone — run after task 2 completes)
  </acceptance_criteria>
  <done>vitest.config.ts include array contains both *.test.ts and *.test.tsx patterns</done>
</task>

<task type="auto">
  <name>Task 2: Update client-result.test.ts — remove D-05 and D-03 violated assertions</name>
  <files>tests/client-result.test.ts</files>
  <read_first>tests/client-result.test.ts</read_first>
  <action>
    Two test sections need updating in `tests/client-result.test.ts`:

    SECTION A — "shows immediate loading feedback before llm candidates resolve" (around line 272):
    This test asserts `writeStreamWrites.join("").toContain("QueQue is thinking...")` at line 299. D-05 removes the raw ANSI write. Update the test so it:
    1. Still sets up the pending `fetchCandidates` promise (keep the setup as-is through line ~295).
    2. Replaces the `writeStreamWrites.join("").toContain("QueQue is thinking...")` assertion with an assertion that verifies the modal resolves after candidates arrive: after calling `resolveCandidates([{ command: "git status", explanation: "" }])` and `await clientPromise`, assert that the result file contains `{kind:"replace-buffer", lbuffer:"git status"}`. This confirms the modal-first async path completes correctly even though the loading indicator is now inside Ink rather than raw stdout.
    3. Keep the polling loop (`for (let i = 0; i < 20 && writeStreamWrites.length === 0; i++)`) but change what it polls for: instead of checking `writeStreamWrites.length`, just wait a fixed `await new Promise(r => setTimeout(r, 50))` for the modal to open, then resolve candidates and await clientPromise.
    4. The test name may remain "shows immediate loading feedback before llm candidates resolve" or be renamed to "resolves modal after llm candidates arrive" — either is acceptable.

    SECTION B — "emits a replace-buffer result in llm mode and the shell applies it" (around line 229):
    This test passes a single-candidate array `[{ command: "git status", explanation: "" }]`. After D-03, the fast-accept bypass is removed and the modal always opens. The current test will hang because `runForegroundClient` will launch an Ink modal and wait for user input. Fix by mocking `ink`'s `render` to immediately invoke the `onSelect` callback:
    Add a `vi.mock('ink', ...)` block at the top of the mocks section (alongside the existing mocks). The mock should export `render: vi.fn().mockImplementation((element) => { ... })`. Inside the mock implementation: extract the `onSelect` prop from the element's props and call `onSelect(element.props.candidates?.[0]?.command ?? 'git status')` after a `await Promise.resolve()` tick. Return `{ unmount: vi.fn(), rerender: vi.fn() }`.

    NOTE: The ink mock should be added with `vi.mock('ink', ...)` in the hoisted mock section (lines 14–102). The existing test structure otherwise remains unchanged — only add the mock and fix the two described test sections.

    After both changes, `pnpm test:run -- tests/client-result.test.ts` must exit 0.
  </action>
  <verify>
    <automated>pnpm test:run -- tests/client-result.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test:run -- tests/client-result.test.ts` exits 0
    - The string `"QueQue is thinking..."` does NOT appear in tests/client-result.test.ts (use: `grep -c 'QueQue is thinking' tests/client-result.test.ts` returns 0)
    - The file still contains the test description "shows immediate loading feedback" or "resolves modal after"
    - The file contains a `vi.mock('ink'` declaration
  </acceptance_criteria>
  <done>All tests in client-result.test.ts pass; no reference to the removed raw ANSI loading text; ink render is mocked to prevent modal hang</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold tests/candidate-select.test.tsx with failing tests for Wave 2</name>
  <files>tests/candidate-select.test.tsx</files>
  <read_first>tests/client-result.test.ts</read_first>
  <action>
    Create `tests/candidate-select.test.tsx` as a new file. This file provides the test contract that Wave 2 must satisfy. Tests should be FAILING after this task (CandidateSelect has the old signature) — that is intentional; Wave 2 makes them green.

    Import: `import React from 'react'`. Import `describe`, `it`, `expect`, `vi` from vitest. Import `render` from `@testing-library/react` — WAIT: this project has no @testing-library/react. Use Ink's own test utilities instead. Actually, since Ink is a TTY renderer and vitest runs in node environment without a real TTY, the simplest approach is to test the component's behavior through unit-level logic tests (filter function, state logic) rather than full render tests. Use a mock-render strategy:

    Mock `ink` entirely: `vi.mock('ink', ...)` returning stub `Box`, `Text`, `useInput`, and `useEffect`/`useState` from actual react. The mock for `useInput` should capture the handler for use in tests.

    Write these specific test cases (these are the behaviors Wave 2 must satisfy):

    1. describe "CandidateSelect — loading state": it "renders spinner placeholder when candidates is null" — import `CandidateSelect`, call it with `candidates={null}`, `onSelect={vi.fn()}`, `onCancel={vi.fn()}`. With Ink mocked, verify the component renders without throwing. This is a smoke test — it passes after Wave 2 adds null-branch rendering.

    2. describe "CandidateSelect — filterCandidates logic": Since `filterCandidates` will be a module-level function, test it directly by importing it if exported, or test it indirectly through the component behavior. Write a test that with `candidates=[{command:'git status',explanation:''},{command:'ls -la',explanation:''}]` and `query='git'`, only the 'git status' candidate is selected by the Enter handler. Use `useInput` mock to simulate Enter key press.

    3. describe "CandidateSelect — keyboard navigation": it "calls onCancel when Escape is pressed" — use the captured `useInput` handler, call it with `('', {escape:true, ...zeroKeys})`, assert `onCancel` was called.

    4. describe "CandidateSelect — D-01 pre-filter": it "pre-populates query from initialQuery prop" — pass `initialQuery="git"` and candidates list, verify that when Enter is pressed, onSelect is called with a git command (not an ls command).

    For `useInput` mock capture: store the handler in a variable: `let capturedInputHandler: ((input: string, key: object) => void) | undefined`. In `vi.mock('ink', ...)`, implement `useInput: vi.fn().mockImplementation((handler) => { capturedInputHandler = handler; })`.

    The test file must import `CandidateSelect` from `'../src/ui/CandidateSelect.js'` with the `.js` extension.

    Helper: define `const zeroKeys = { upArrow:false, downArrow:false, escape:false, return:false, backspace:false, ctrl:false, meta:false, shift:false, tab:false, delete:false, pageDown:false, pageUp:false, home:false, end:false }`.
  </action>
  <verify>
    <automated>pnpm test:run -- tests/candidate-select.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `tests/candidate-select.test.tsx` file exists
    - `pnpm test:run -- tests/candidate-select.test.tsx` runs (does not crash vitest itself) — individual tests may fail (RED is expected at this wave)
    - File contains the string `CandidateSelect` as an import
    - File contains at least 3 `it(` or `test(` blocks
    - `grep -c "vi.mock('ink'" tests/candidate-select.test.tsx` returns at least 1
  </acceptance_criteria>
  <done>tests/candidate-select.test.tsx exists, is discovered by vitest (config updated in Task 1), contains ink mock and 3+ test cases targeting Wave 2 behavior. Tests are intentionally RED pending Wave 2 implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → source | Tests import source modules; mocks prevent real I/O |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-031-01 | Tampering | vitest.config.ts | accept | Config is developer-owned; not a runtime attack surface |
| T-031-02 | Tampering | tests/candidate-select.test.tsx | accept | Test files run only in CI/dev; not shipped to end users |
| T-031-03 | Information Disclosure | test mock of fetchCandidates | accept | Mock returns controlled data; no real API keys in test env |
</threat_model>

<verification>
After all three tasks:
- `pnpm test:run` exits 0 for all existing tests
- `pnpm test:run -- tests/candidate-select.test.tsx` runs (may have failing tests — that is expected)
- `grep -v '^//' tests/client-result.test.ts | grep -c 'QueQue is thinking'` returns 0
- `grep -c 'test\.tsx' vitest.config.ts` returns 1
</verification>

<success_criteria>
- vitest.config.ts discovers .test.tsx files
- tests/client-result.test.ts is fully green with no raw-ANSI-loading or fast-accept assertions
- tests/candidate-select.test.tsx exists and is viable (vitest does not crash on it)
- pnpm test:run exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-update-interface-and-interactivity-to-match-that-of-this-git/03.1-01-SUMMARY.md`
</output>

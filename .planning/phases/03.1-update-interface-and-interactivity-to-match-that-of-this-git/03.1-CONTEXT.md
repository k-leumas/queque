# Phase 3.1: Monocle-style Interface and Interactivity - Context

**Gathered:** 2026-05-13
**Status:** Ready for planning

<domain>
## Phase Boundary

Redesign the existing TUI components (`CandidateSelect`, `Modal`) to match the monocle terminal UI model. Visual design is fully locked in `03.1-UI-SPEC.md`. This context captures implementation decisions not covered by the UI-SPEC: loading state integration, single-candidate behavior, and initial search filter behavior.

</domain>

<decisions>
## Implementation Decisions

### Initial Search Filter

- **D-01:** If `request.lbuffer` is non-empty when the modal opens, candidates are pre-filtered by that text on first render. The selected index resets to 0 after the pre-filter is applied.
- **D-02:** If `request.lbuffer` is empty, all LLM-returned candidates are shown without filtering. The search box shows the placeholder copy (`type to filter…`).

### Single-Candidate Behavior

- **D-03:** The existing fast-accept bypass (1 candidate → direct buffer write, no modal) is **removed**. The modal always shows regardless of how many candidates the provider returns.
- **D-04:** The best candidate is first in the list (index 0, preselected on load). Sort order is owned by the provider — `CandidateSelect` renders candidates as received, no UI-side re-ranking.

### Loading State Integration

- **D-05:** The modal opens **immediately** with a spinner (before `fetchCandidates` returns). The raw ANSI `"Que-Que is thinking..."` pre-modal text in `run-foreground.ts` is removed.
- **D-06:** `CandidateSelect` receives `candidates: CandidateList | null`. When `null`, the component renders the spinner zone. When `CandidateList`, it renders the candidate rows. Error state per UI-SPEC copywriting contract (`provider error — press esc to return`).
- **D-07:** `run-foreground.ts` passes `candidates={null}` on first render, then pushes the resolved `CandidateList` into the already-rendered Ink modal via a nullable prop update (React ref or setState callback pattern — Claude's discretion).

### Claude's Discretion

- React state/ref pattern for propagating the candidate update into the already-rendered Ink modal
- `SearchInput`, `ControlsLine`, `LoadingSpinner` component internals (UI-SPEC defines the external contract)
- `MODAL_CHROME_LINES` constant update in `renderModal` to account for search zone, gaps, and controls zone
- Filter algorithm internals (case-insensitive substring per UI-SPEC §Live Search)
- Selected index reset behavior when filter produces 0 matches

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Visual Design Contract
- `.planning/phases/03.1-update-interface-and-interactivity-to-match-that-of-this-git/03.1-UI-SPEC.md` — **Primary spec.** Locks colors, glyphs (`┌>`), layout zones (header/search/results/controls), component inventory, keyboard map, animation spec, copywriting contract, and npm safety gate. Read this before touching any UI file.

### Files to Modify
- `src/ui/Modal.tsx` — Modal shell; increase default width 62 → 80; add search zone slot
- `src/ui/CandidateSelect.tsx` — Main component; replace `❯` with `┌>`, add live search input, add loading state prop, update colors to monocle palette

### Files to Create
- `src/ui/SearchInput.tsx` — Renders `SEARCH: {query}` row; manages query string state
- `src/ui/ControlsLine.tsx` — Styled controls footer with key badge + action text pattern
- `src/ui/LoadingSpinner.tsx` — Dot-cycle spinner for async wait states

### Integration Point
- `src/client/run-foreground.ts` — Foreground client; remove fast-accept bypass and raw ANSI spinner; refactor to modal-first async rendering with `candidates: CandidateList | null`

### Contracts
- `src/contracts/candidates.ts` — `CandidateList` type consumed by `CandidateSelect`

### Requirements
- `.planning/REQUIREMENTS.md` §TUI Experience — TUI-01 (fuzzy-finder-style TUI with keyboard focus) is the requirement this phase advances

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `src/ui/Modal.tsx:Modal` — existing rounded-border container; keep `borderColor="cyan"`, `paddingX={1}`, `title` prop; increase default `width` from 62 to 80
- `src/ui/CandidateSelect.tsx:CandidateSelect` — existing keyboard nav (`useInput`) and wrapping behavior; preserve `onSelect`/`onCancel` callbacks; extend with search query state and `candidates: CandidateList | null` prop
- `src/client/run-foreground.ts:renderModal` — existing sticky-bottom viewport scroll logic; update `MODAL_CHROME_LINES` (currently 7; needs adjustment for new search + controls zones)

### Established Patterns
- Zod schemas for all cross-boundary contracts (from Phase 2)
- Named exports with module-level functions — new components follow `export function ComponentName`
- `appendDebugLog` for tracing without blocking UI (from Phase 2)

### Integration Points
- `src/client/run-foreground.ts` (llm path) → modal-first render with `candidates={null}` → `fetchCandidates` resolves → modal updates with `CandidateList`
- `request.lbuffer` in `run-foreground.ts` → passed as `initialQuery` prop to `CandidateSelect` → drives initial filter state (D-01/D-02)
- Error path: `fetchCandidates` throws → modal updates to error state → user presses Esc → `writeShellResult({kind:'cancel'})`

</code_context>

<specifics>
## Specific Ideas

- Monocle reference: https://github.com/imsnif/monocle — the visual model this phase targets
- Selection glyph: `┌>` (active first line) + `│ ` (active continuation) instead of `❯`; `  ` (2 spaces) for inactive rows
- Modal inner width: 76 chars at 80-col modal (`width - 4`)

</specifics>

<deferred>
## Deferred Ideas

- `Ctrl+F` toggle (floating/tiled mode) — UI-SPEC explicitly defers to future phase
- `Ctrl+R` toggle (filter type) — UI-SPEC explicitly defers to future phase
- Full fuzzy matching (vs. substring) — Phase 4 scope per REQUIREMENTS TUI-01

</deferred>

---

*Phase: 03.1-update-interface-and-interactivity-to-match-that-of-this-git*
*Context gathered: 2026-05-13*

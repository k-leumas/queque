---
phase: 03
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/claude-provider.test.ts
  - tests/shell-contract.test.ts
  - src/contracts/shell.ts
  - src/providers/provider.ts
  - src/providers/claude.ts
autonomous: true
requirements:
  - PRV-01
  - PRV-02
  - PRV-03
  - CMD-01
  - CMD-02

must_haves:
  truths:
    - "src/providers/provider.ts exports a LLMAdapter interface with a single fetchCandidates method"
    - "src/providers/claude.ts exports claudeAdapter satisfying LLMAdapter and fetchCandidates as a named function"
    - "claude.ts resolves model from QQ_MODEL env or DEFAULT_MODEL constant (no runtime API poll)"
    - "shellResultSchema accepts error kind with message string"
    - "tests/claude-provider.test.ts has no modelListMock references and asserts default model is claude-haiku-4-5-20251001"
    - "tests/shell-contract.test.ts has positive test for error ShellResult variant"
    - "pnpm test:run passes green after all changes in this plan"
  artifacts:
    - path: "src/providers/provider.ts"
      provides: "LLMAdapter interface"
      exports: ["LLMAdapter"]
    - path: "src/providers/claude.ts"
      provides: "Claude LLMAdapter implementor with simplified model selection"
      exports: ["fetchCandidates", "claudeAdapter"]
    - path: "src/contracts/shell.ts"
      provides: "shellResultSchema with error variant"
      contains: "z.literal('error')"
    - path: "tests/claude-provider.test.ts"
      provides: "Updated tests — no modelListMock, asserts haiku default"
    - path: "tests/shell-contract.test.ts"
      provides: "Positive test for error ShellResult variant"
  key_links:
    - from: "src/providers/claude.ts"
      to: "src/providers/provider.ts"
      via: "import type { LLMAdapter }"
      pattern: "LLMAdapter"
    - from: "src/providers/claude.ts"
      to: "fetchCandidates call"
      via: "single client.messages.create call without model loop"
      pattern: "resolveModel"
    - from: "src/contracts/shell.ts"
      to: "writeShellResult in result-writer.ts"
      via: "shellResultSchema.parse()"
      pattern: "kind.*error"
---

<objective>
Stand up the formal LLMAdapter contract, implement Claude behind it, and simplify model selection. Also extend shellResultSchema with the error variant (required by Plan 02 for writeShellResult to accept error results without code changes). Run updated tests that go RED first (Wave 0 TDD obligation) then GREEN after implementation.

Purpose: Establish the provider abstraction (PRV-02, PRV-03) and Claude's wiring to it (PRV-01, CMD-01, CMD-02). The shellResultSchema extension in this plan unblocks Plan 02 (error kind write path) and Plan 03 (ZSH widget) in parallel.
Output: src/providers/provider.ts (new), claude.ts refactored, shellResultSchema extended, tests updated and green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-claude-fast-path-and-ranked-suggestions/03-CONTEXT.md
@.planning/phases/03-claude-fast-path-and-ranked-suggestions/03-RESEARCH.md
@.planning/phases/03-claude-fast-path-and-ranked-suggestions/03-PATTERNS.md
</context>

<interfaces>
<!-- Key contracts the executor must respect. Extracted from codebase. -->

From src/contracts/request.ts:
  export type ContextEnvelope — fully assembled context passed to the provider adapter;
  has `base: BaseContext` and `extras: ContextChunk[]`

From src/contracts/candidates.ts (unchanged per D-13):
  export type CandidateList = Array<{ command: string; explanation: string }>
  export const candidateListSchema — zod schema, 1–5 items, each item { command, explanation }

From src/contracts/shell.ts (current state — will be extended by this plan):
  export const shellResultSchema = z.discriminatedUnion('kind', [
    z.object({ kind: z.literal('cancel') }).strict(),
    z.object({ kind: z.literal('replace-buffer'), lbuffer: z.string(), rbuffer: z.string() }),
  ])

From src/context/provider.ts (analog pattern for LLMAdapter):
  export interface ContextProvider {
    id: string;
    intents: Array<RequestIntent | '*'>;
    gather(input: GatherContextInput): Promise<ContextChunk | null>;
  }

From src/shared/env-file.ts:
  export function readEnvValueFromDotEnvLocal(key: string): string | undefined

From src/shared/debug-log.ts:
  export function appendDebugLog(category: string, event: string, data?: unknown): Promise<void>
  // Call as: void appendDebugLog('provider', 'event-name', { ... })
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update failing tests — remove modelListMock, add error variant tests</name>
  <files>tests/claude-provider.test.ts, tests/shell-contract.test.ts</files>
  <read_first>
    - tests/claude-provider.test.ts (full file — read current modelListMock setup, vi.hoisted block, AnthropicMock class, beforeEach, all 4 test cases)
    - tests/shell-contract.test.ts (full file — read existing positive test patterns)
    - .planning/phases/03-claude-fast-path-and-ranked-suggestions/03-PATTERNS.md (sections on test file patterns — exact code excerpts for what to remove and add)
  </read_first>
  <behavior>
    claude-provider.test.ts:
    - Test 1 (updated): fetchCandidates with haiku — no modelListMock call, createMock returns candidates, assert request.model === 'claude-haiku-4-5-20251001' (not 'claude-3-haiku-20240307')
    - Test 2 (new): QQ_MODEL env override — set process.env.QQ_MODEL = 'claude-custom', call fetchCandidates, assert request.model === 'claude-custom'
    - Test 3 (kept/updated): malformed JSON fallback — no modelListMock, createMock returns 'not json', result is [{ command: 'not json', explanation: '' }]
    - Test 4 (kept/updated): model error throws — no modelListMock, createMock rejects with '404 model not found'
    - Test 5 (kept/updated): QQ_FORCE_SELECTOR padding — no modelListMock, createMock returns single candidate, result has length 2
    shell-contract.test.ts:
    - Test A (new): error variant accepted — shellResultSchema.safeParse({ kind: 'error', message: 'QueQue: API timeout — press any key' }) succeeds
    - Test B (new): error without message rejected — shellResultSchema.safeParse({ kind: 'error' }) fails
  </behavior>
  <action>
    In tests/claude-provider.test.ts:
    (a) Remove modelListMock from vi.hoisted() — keep createMock and anthropicCtorMock only.
    (b) Remove `models: { list: typeof modelListMock }` from AnthropicMock class definition; remove the `this.models = { list: modelListMock }` line from the constructor.
    (c) Remove `modelListMock.mockReset()` from beforeEach; add `delete process.env.QQ_MODEL` if not already present (it is on line 49 — verify and keep).
    (d) Update the first test ("returns candidate JSON from Claude..."): remove the `modelListMock.mockImplementation(async function* ...)` call, remove `expect(modelListMock).toHaveBeenCalledTimes(1)`, change the model assertion from `'claude-3-haiku-20240307'` to `'claude-haiku-4-5-20251001'`.
    (e) Update tests 2–4 similarly: remove all modelListMock.mockImplementation calls.
    (f) Add a new test for QQ_MODEL override: set `process.env.QQ_MODEL = 'claude-custom'` before calling fetchCandidates, assert `createMock.mock.calls[0][0].model === 'claude-custom'`, then `delete process.env.QQ_MODEL` in afterEach or rely on beforeEach cleanup.
    In tests/shell-contract.test.ts:
    (a) Add inside the existing `describe('shellResultSchema', ...)` block, after existing tests:
        - Test: safeParse({ kind: 'error', message: 'QueQue: API timeout — press any key' }).success === true, and data.kind === 'error'
        - Test: safeParse({ kind: 'error' }).success === false (message required)
    Run `pnpm test:run -- tests/claude-provider.test.ts tests/shell-contract.test.ts` — these MUST fail (RED) because the source is not yet updated.
  </action>
  <verify>
    <automated>pnpm --dir /Users/samuel/dev/tui-llm test:run -- tests/claude-provider.test.ts tests/shell-contract.test.ts 2>&1 | grep -E 'FAIL|fail|Error' | head -20</automated>
  </verify>
  <done>
    Both test files have been updated and run RED (failing) because the production code does not yet implement the new behavior. Failure messages reference: model assertion mismatch OR modelListMock usage OR error variant parse failure.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create LLMAdapter interface (src/providers/provider.ts)</name>
  <files>src/providers/provider.ts</files>
  <read_first>
    - src/context/provider.ts (analog — exact interface pattern this project uses: import type, .js extension, no default export, interface only)
    - src/contracts/candidates.ts (to confirm CandidateList export path and name)
    - src/contracts/request.ts (to confirm ContextEnvelope export path and name)
  </read_first>
  <action>
    Create src/providers/provider.ts with:
    - `import type { CandidateList } from '../contracts/candidates.js'`
    - `import type { ContextEnvelope } from '../contracts/request.js'`
    - Export a single interface `LLMAdapter` with one method: `fetchCandidates(envelope: ContextEnvelope): Promise<CandidateList>`
    No default export. No class. No runtime code. File must not exist yet at this path (verified — only claude.ts exists in src/providers/).
    Verify file compiles: `pnpm build` must emit no TypeScript errors related to the new file.
  </action>
  <verify>
    <automated>pnpm --dir /Users/samuel/dev/tui-llm build 2>&1 | grep -v '^$' | tail -5</automated>
  </verify>
  <done>
    src/providers/provider.ts exists, exports interface LLMAdapter with fetchCandidates method, pnpm build succeeds.
    grep check: grep -c 'export interface LLMAdapter' /Users/samuel/dev/tui-llm/src/providers/provider.ts outputs 1.
  </done>
</task>

<task type="auto">
  <name>Task 3: Refactor claude.ts and extend shellResultSchema — then go GREEN</name>
  <files>src/providers/claude.ts, src/contracts/shell.ts</files>
  <read_first>
    - src/providers/claude.ts (full file — know current model selection logic lines 8–46, fetchCandidates lines 132–183, suggestShellResult lines 185–197, imports lines 1–6)
    - src/contracts/shell.ts (full file — know current discriminatedUnion shape before extending)
    - .planning/phases/03-claude-fast-path-and-ranked-suggestions/03-PATTERNS.md (sections on claude.ts and shell.ts patterns — exact code excerpts for what to delete and replace)
    - src/providers/provider.ts (just created — import path for LLMAdapter)
  </read_first>
  <action>
    In src/contracts/shell.ts:
    Append a third variant to the shellResultSchema discriminatedUnion array:
      `z.object({ kind: z.literal('error'), message: z.string() })`
    Update the comment block above the schema to note the three variants (cancel, replace-buffer, error).
    Re-infer `export type ShellResult` from the updated schema — the type alias line is unchanged, it will pick up the new variant automatically.

    In src/providers/claude.ts:
    (a) REMOVE imports: the `ShellResult` / `shellResultSchema` import (line 4) — used only by suggestShellResult.
    (b) ADD import: `import type { LLMAdapter } from './provider.js'`
    (c) DELETE lines 8–46 entirely: DEFAULT_MODEL constant ('claude-sonnet-4-0'), CHEAPEST_FIRST_MODEL_IDS array, listAvailableModelIds function, chooseCheapestAvailableModel function, getCandidateModels function.
    (d) ADD in their place:
        `const DEFAULT_MODEL = 'claude-haiku-4-5-20251001';`
        `function resolveModel(): string { return process.env.QQ_MODEL ?? readEnvValueFromDotEnvLocal('QQ_MODEL') ?? DEFAULT_MODEL; }`
    (e) In fetchCandidates: remove `const models = await getCandidateModels(client)` and the `for (const model of models) {` loop wrapper. Replace with a single call using `const model = resolveModel()`. The `try/catch` wrapping the messages.create call stays — just no loop.
    (f) Update the system prompt to: 'You are QueQue, a terminal shell assistant. Return ONLY a JSON array of command candidates, ranked with the most correct/direct command first. No prose, no markdown, no code fences.'
    (g) Update the `appendDebugLog('provider', 'request start', ...)` call: change the `models` key to `model` (singular string, not array).
    (h) DELETE suggestShellResult function (lines 185–197) entirely — zero external callers confirmed by RESEARCH.md.
    (i) ADD after fetchCandidates: `export const claudeAdapter: LLMAdapter = { fetchCandidates };`
    Keep all other functions unchanged: extractText, parseCandidates, shouldForceSelector, ensureSelectableCandidates, buildPrompt.

    Run tests to verify GREEN:
    `pnpm test:run -- tests/claude-provider.test.ts tests/shell-contract.test.ts`
  </action>
  <verify>
    <automated>pnpm --dir /Users/samuel/dev/tui-llm test:run -- tests/claude-provider.test.ts tests/shell-contract.test.ts</automated>
  </verify>
  <done>
    pnpm test:run passes for both test files (all tests green).
    grep -c "suggestShellResult" /Users/samuel/dev/tui-llm/src/providers/claude.ts outputs 0.
    grep -c "CHEAPEST_FIRST" /Users/samuel/dev/tui-llm/src/providers/claude.ts outputs 0.
    grep -c "claude-haiku-4-5-20251001" /Users/samuel/dev/tui-llm/src/providers/claude.ts outputs 1.
    grep -c "claudeAdapter" /Users/samuel/dev/tui-llm/src/providers/claude.ts outputs 1.
    grep -c "z.literal('error')" /Users/samuel/dev/tui-llm/src/contracts/shell.ts outputs 1.
    pnpm build succeeds with no TypeScript errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| claude.ts → Anthropic API | Untrusted JSON response from external LLM; parsed through candidateListSchema before use |
| env → claude.ts | QQ_MODEL env var sets model string; no shell injection risk (string is passed as API parameter only, not executed) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | parseCandidates in claude.ts | mitigate | candidateListSchema.parse() rejects non-conforming Claude responses; malformed text falls back to echo "" |
| T-03-02 | Information Disclosure | appendDebugLog in claude.ts | accept | ANTHROPIC_API_KEY is passed to Anthropic constructor only, never to appendDebugLog — existing behavior preserved |
| T-03-03 | Tampering | shellResultSchema error variant | mitigate | error kind is validated by shellResultSchema.parse() before FIFO write; message field is string only (no executable payload) |
</threat_model>

<verification>
After all three tasks:
- pnpm test:run passes all tests (entire suite green)
- pnpm build succeeds with no TypeScript errors
- src/providers/provider.ts exists and exports LLMAdapter interface
- src/providers/claude.ts exports claudeAdapter (LLMAdapter) and fetchCandidates (named function)
- src/contracts/shell.ts discriminatedUnion has three variants: cancel, replace-buffer, error
- No modelListMock references remain in tests/claude-provider.test.ts
- Model assertion in tests is 'claude-haiku-4-5-20251001'
</verification>

<success_criteria>
- pnpm test:run exits 0 (all tests pass)
- pnpm build exits 0 (no TypeScript errors)
- LLMAdapter interface is the single contract for provider adapters
- Claude uses claude-haiku-4-5-20251001 unless QQ_MODEL overrides it
- shellResultSchema accepts { kind: 'error', message: string }
- suggestShellResult is gone from claude.ts
</success_criteria>

<output>
After completion, create .planning/phases/03-claude-fast-path-and-ranked-suggestions/03-01-SUMMARY.md
</output>

---
phase: 03
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - shell/zsh/qq.zsh
  - tests/zsh-widget.test.ts
autonomous: true
requirements:
  - SAFE-01

must_haves:
  truths:
    - "_qq_apply_result handles error kind: restores QQ_ORIG_LBUFFER and QQ_ORIG_RBUFFER and returns 0"
    - "qq-question-widget inline case block handles error kind: restores original buffers"
    - "tests/zsh-widget.test.ts has a test for _qq_apply_result with error kind returning 0 and leaving buffers unchanged"
    - "pnpm test:run passes green after all changes in this plan"
  artifacts:
    - path: "shell/zsh/qq.zsh"
      provides: "error case in both _qq_apply_result and qq-question-widget"
      contains: "error)"
    - path: "tests/zsh-widget.test.ts"
      provides: "Test: _qq_apply_result with error kind no-ops buffer mutation"
  key_links:
    - from: "shell/zsh/qq.zsh _qq_apply_result"
      to: "qq-question-widget case block"
      via: "both must handle error) case identically"
      pattern: "error\\)"
---

<objective>
Add the `error)` case to both the `_qq_apply_result` helper function and the `qq-question-widget` inline case block in qq.zsh. Add a test to zsh-widget.test.ts verifying that _qq_apply_result no-ops buffer mutation on error kind.

Purpose: SAFE-01 — the shell buffer must never be mutated when the provider returns an error. The error ShellResult written by Plan 02 reaches the ZSH widget via FIFO; the widget must handle it by restoring original buffers and returning control to the user. Both case blocks must be updated because they serve different code paths (tests use _qq_apply_result; runtime uses qq-question-widget inline block).
Output: qq.zsh with explicit error handling, zsh-widget.test.ts with new test, full suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-claude-fast-path-and-ranked-suggestions/03-CONTEXT.md
@.planning/phases/03-claude-fast-path-and-ranked-suggestions/03-RESEARCH.md
@.planning/phases/03-claude-fast-path-and-ranked-suggestions/03-PATTERNS.md
@.planning/phases/03-claude-fast-path-and-ranked-suggestions/03-01-SUMMARY.md
</context>

<interfaces>
<!-- Key contracts the executor must respect. Extracted from codebase. -->

From shell/zsh/qq.zsh (current _qq_apply_result case block, lines 95–121):
  case "$kind" in
    cancel)
      LBUFFER="$QQ_ORIG_LBUFFER"
      RBUFFER="$QQ_ORIG_RBUFFER"
      return 0
      ;;
    replace-buffer)
      # ... jq parse and apply ...
      ;;
    *)
      LBUFFER="$QQ_ORIG_LBUFFER"
      RBUFFER="$QQ_ORIG_RBUFFER"
      return 1        # NOTE: wildcard returns 1 (failure), error) must return 0 (expected kind)
      ;;
  esac

From shell/zsh/qq.zsh (current qq-question-widget inline case block, lines 204–220):
  case "$kind" in
    cancel)
      LBUFFER="$QQ_ORIG_LBUFFER"
      RBUFFER="$QQ_ORIG_RBUFFER"
      ;;
    replace-buffer)
      # ... jq parse and apply ...
      ;;
    *)
      LBUFFER="$QQ_ORIG_LBUFFER"
      RBUFFER="$QQ_ORIG_RBUFFER"
      ;;
  esac

From tests/zsh-widget.test.ts (pattern — how _qq_apply_result is tested via spawnSync zsh -f -c):
  The test pattern from client-result.test.ts shows how _qq_apply_result is called:
    source shell/zsh/qq.zsh
    QQ_ORIG_LBUFFER="original left"
    QQ_ORIG_RBUFFER="original right"
    LBUFFER="modified"
    RBUFFER=""
    _qq_apply_result "$result_file"
    echo "exit=$?"
    echo "lbuffer=$LBUFFER"
    echo "rbuffer=$RBUFFER"
  zsh-widget.test.ts uses spawnSync('zsh', [...]) and writes the result JSON to a temp file first.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing test for error kind in zsh-widget.test.ts</name>
  <files>tests/zsh-widget.test.ts</files>
  <read_first>
    - tests/zsh-widget.test.ts (full file — understand current test structure, how _qq_apply_result is tested via spawnSync, what beforeEach/afterEach setup exists, how temp files are created and cleaned up)
    - .planning/phases/03-claude-fast-path-and-ranked-suggestions/03-PATTERNS.md (section on ZSH widget error case — exact case block code to add)
  </read_first>
  <behavior>
    - Test: write { kind: 'error', message: 'QueQue: API timeout — press any key' } to a temp file, call _qq_apply_result with that file
    - Assert: LBUFFER is restored to QQ_ORIG_LBUFFER (not mutated)
    - Assert: RBUFFER is restored to QQ_ORIG_RBUFFER (not mutated)
    - Assert: _qq_apply_result exits 0 (error is a known/expected kind, not a failure)
  </behavior>
  <action>
    Read tests/zsh-widget.test.ts to understand the existing test structure and temp file usage. Add a new test (inside the existing _qq_apply_result describe block or alongside it) that:
    (a) Writes `{"kind":"error","message":"QueQue: API timeout — press any key"}` to a temp result file (follow the exact temp file creation pattern used by existing tests).
    (b) Calls `_qq_apply_result "$result_file"` via spawnSync zsh -f -c, with QQ_ORIG_LBUFFER set to a known value (e.g. 'original left').
    (c) Asserts exit code is 0 (not 1 — error is a recognized kind).
    (d) Asserts stdout contains 'lbuffer=original left' (buffer restored).
    (e) Asserts stdout contains 'rbuffer=original right' (buffer restored).
    Run `pnpm test:run -- tests/zsh-widget.test.ts` — this MUST fail RED because qq.zsh does not yet have the error) case (the `*` wildcard returns 1, not 0).
  </action>
  <verify>
    <automated>pnpm --dir /Users/samuel/dev/tui-llm test:run -- tests/zsh-widget.test.ts 2>&1 | grep -E 'FAIL|fail|Error' | head -10</automated>
  </verify>
  <done>
    New test runs and fails (exit code assertion fails — _qq_apply_result returns 1 for unknown kind via wildcard). All other tests in zsh-widget.test.ts remain green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add error) case to both _qq_apply_result and qq-question-widget in qq.zsh — then go GREEN</name>
  <files>shell/zsh/qq.zsh</files>
  <read_first>
    - shell/zsh/qq.zsh (full file — locate BOTH case blocks: _qq_apply_result lines 95–121 and qq-question-widget lines 204–220; understand exact indentation and separator style used by existing cases)
    - .planning/phases/03-claude-fast-path-and-ranked-suggestions/03-PATTERNS.md (section on qq.zsh — exact zsh code for the error) case in both locations, and note on Pitfall 4: both locations MUST be updated)
  </read_first>
  <action>
    In shell/zsh/qq.zsh, make two changes — BOTH are required (Pitfall 4 from RESEARCH.md):

    Change 1 — _qq_apply_result case block (lines 95–121):
    Insert an `error)` case BEFORE the `*) ` wildcard:
      error)
        # Error kind — provider failure; restore original buffers without mutating the shell line (D-11)
        LBUFFER="$QQ_ORIG_LBUFFER"
        RBUFFER="$QQ_ORIG_RBUFFER"
        return 0
        ;;
    The wildcard `*)` continues to return 1 for truly unknown kinds. The error case returns 0 because it is an expected, handled kind.

    Change 2 — qq-question-widget inline case block (lines 204–220):
    Insert an `error)` case BEFORE the `*) ` wildcard:
      error)
        # Error kind — provider failure; restore original buffers (D-11)
        LBUFFER="$QQ_ORIG_LBUFFER"
        RBUFFER="$QQ_ORIG_RBUFFER"
        ;;
    No return statement needed in the widget block (widget does not return meaningful exit codes to ZLE).

    Do NOT change any other parts of the file (trap logic, prewarm, binding, buffer capture, JSON escaping).

    Run full test suite:
    `pnpm test:run`
  </action>
  <verify>
    <automated>pnpm --dir /Users/samuel/dev/tui-llm test:run</automated>
  </verify>
  <done>
    pnpm test:run exits 0 (all tests pass including the new error kind test).
    grep -c 'error)' /Users/samuel/dev/tui-llm/shell/zsh/qq.zsh outputs 2 (one per case block).
    Manual spot check (no Zellij required): source qq.zsh in a non-interactive zsh, write {"kind":"error","message":"test"} to a temp file, call _qq_apply_result — exits 0 and leaves buffers unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FIFO result → ZSH case block | JSON line arrives from qq client; kind field controls buffer mutation decision |
| error message field → ZSH | message string is read but NEVER written to LBUFFER/RBUFFER in error case |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | qq-question-widget error case | mitigate | error) case explicitly restores original buffers; message field is not read or applied (mitigates Tamper-2: shell metacharacters in message cannot reach buffer) |
| T-03-08 | Tampering | _qq_apply_result error case | mitigate | Same as T-03-07; buffer restoration is explicit and return 0 marks the kind as known-safe without mutation |
</threat_model>

<verification>
After both tasks:
- pnpm test:run exits 0 (full suite green)
- shell/zsh/qq.zsh contains exactly 2 occurrences of 'error)' (one per case block)
- _qq_apply_result returns 0 for error kind
- qq-question-widget restores buffers on error kind
- No buffer mutation occurs on error ShellResult (end-to-end: Plan 02 writes error to FIFO, Plan 03 no-ops mutation)
</verification>

<success_criteria>
- pnpm test:run exits 0
- Both case blocks in qq.zsh explicitly handle error kind
- Buffer is never mutated on provider error (SAFE-01 fulfilled end-to-end across Plans 01–03)
- zsh-widget.test.ts new test passes: _qq_apply_result exits 0 and restores original buffers on error kind
</success_criteria>

<output>
After completion, create .planning/phases/03-claude-fast-path-and-ranked-suggestions/03-03-SUMMARY.md
</output>

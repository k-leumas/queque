---
phase: 03
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/contracts/request.ts
  - src/client/run-foreground.ts
  - src/registry/bootstrap.ts
  - tests/client-result.test.ts
autonomous: true
requirements:
  - PRV-02
  - SAFE-01

must_haves:
  truths:
    - "NormalizedRequest carries a confidence field (number 0–1) sourced from classifyIntent() IntentDecision"
    - "run-foreground.ts outer catch writes { kind: 'error', message: 'QueQue: <reason> — press any key' } instead of { kind: 'cancel' }"
    - "run-foreground.ts inner fetchCandidates .catch() calls appendDebugLog with the error message"
    - "bootstrapBuiltins() registers a provider backend descriptor for claude"
    - "tests/client-result.test.ts has a test proving error ShellResult is written when fetchCandidates rejects"
    - "pnpm test:run passes green after all changes in this plan"
  artifacts:
    - path: "src/contracts/request.ts"
      provides: "normalizedRequestSchema with confidence field"
      contains: "confidence: z.number().min(0).max(1)"
    - path: "src/client/run-foreground.ts"
      provides: "confidence propagation and error ShellResult on failure"
      contains: "kind: 'error'"
    - path: "src/registry/bootstrap.ts"
      provides: "Claude provider backend descriptor registered"
      contains: "registerProviderBackend"
    - path: "tests/client-result.test.ts"
      provides: "Test for error ShellResult on fetchCandidates rejection"
  key_links:
    - from: "classifyIntent() result"
      to: "NormalizedRequest.confidence"
      via: "run-foreground.ts line 96 construction site"
      pattern: "confidence: decision.confidence"
    - from: "run-foreground.ts outer catch"
      to: "writeShellResult"
      via: "error kind"
      pattern: "kind.*error.*QueQue"
---

<objective>
Wire the confidence field from classifyIntent into NormalizedRequest, change the run-foreground outer catch from writing cancel to writing a typed error ShellResult, and register the Claude backend descriptor in bootstrapBuiltins. Add the client-result test proving error propagation.

Purpose: SAFE-01 (errors surface without mutating shell buffer via typed error kind), PRV-02 (provider registration through bootstrap). Confidence field is needed for Phase 5's 0.8 routing gate (INT-04) — adding it now avoids a schema migration later.
Output: Updated request.ts, run-foreground.ts, bootstrap.ts, and client-result test green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-claude-fast-path-and-ranked-suggestions/03-CONTEXT.md
@.planning/phases/03-claude-fast-path-and-ranked-suggestions/03-RESEARCH.md
@.planning/phases/03-claude-fast-path-and-ranked-suggestions/03-PATTERNS.md
@.planning/phases/03-claude-fast-path-and-ranked-suggestions/03-01-SUMMARY.md
</context>

<interfaces>
<!-- Key contracts the executor must respect. Extracted from codebase. -->

From src/contracts/request.ts (current state after plan 01 leaves it — plan 01 does NOT touch this file):
  export const normalizedRequestSchema = shellRequestSchema.extend({
    intent: requestIntentSchema,
  })
  export type NormalizedRequest = z.infer<typeof normalizedRequestSchema>
  export const intentDecisionSchema = z.object({
    intent: requestIntentSchema,
    confidence: z.number().min(0).max(1),   // already on IntentDecision
    signals: z.array(z.string()),
  })
  export type IntentDecision = z.infer<typeof intentDecisionSchema>

From src/client/run-foreground.ts (line 96 — NormalizedRequest construction site):
  const normalized: NormalizedRequest = { ...request, intent: decision.intent };
  // MUST become: { ...request, intent: decision.intent, confidence: decision.confidence }

From src/client/run-foreground.ts (lines 178–184 — outer catch to change):
  } catch (error) {
    const message = error instanceof Error ? error.message : String(error);
    void appendDebugLog('client', 'llm request failed; falling back to cancel', { message });
    await writeShellResult(resultFile, { kind: 'cancel' });
  }

From src/client/run-foreground.ts (lines 170–174 — inner .catch() stays but message format per D-12):
  .catch((err) => {
    const message = err instanceof Error ? err.message : String(err);
    void appendDebugLog('client', 'llm request failed', { message });
    app.rerender(buildCandidateElement(null, true));  // renders error state in Ink UI
  })

From src/registry/provider-backends.ts:
  export function registerProviderBackend(descriptor: ProviderBackendDescriptor): void
  export interface ProviderBackendDescriptor { id: string; name: string; description: string; }

From src/registry/bootstrap.ts (current bootstrapBuiltins — add one call here):
  export function bootstrapBuiltins(): void  // idempotent via bootstrapped flag
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing test for error ShellResult in client-result.test.ts</name>
  <files>tests/client-result.test.ts</files>
  <read_first>
    - tests/client-result.test.ts (read lines 322–361 — the existing llm-mode test "emits a replace-buffer result" is the direct analog; understand mock setup and runForegroundClient call pattern)
    - .planning/phases/03-claude-fast-path-and-ranked-suggestions/03-PATTERNS.md (section on client-result.test.ts — exact test code for error case)
  </read_first>
  <behavior>
    - Test: when `fetchCandidates` rejects with `new Error('API timeout')`, runForegroundClient writes a ShellResult to resultFile with kind === 'error'
    - Test: the written error message contains 'API timeout' (the original error reason)
    - Test: the written error message contains 'QueQue:' (D-12 format)
    - Test: the result does NOT have kind === 'cancel'
  </behavior>
  <action>
    Read the existing llm-mode test at lines 322–361 to understand the `fetchCandidates` mock pattern and result file read pattern. Add a new test inside the same describe block ("runForegroundClient: non-Zellij branch" or the block containing the llm test):
    - Mock `fetchCandidates` to reject with `new Error('API timeout')` via `vi.mocked(fetchCandidates).mockRejectedValue(...)`
    - Call `runForegroundClient({ requestFile, resultFile, resultMode: 'llm' })`
    - Read resultFile with fs.readFileSync, JSON.parse the trimmed content
    - Assert: `parsed.kind === 'error'`
    - Assert: `parsed.message` includes 'API timeout' AND includes 'QueQue:'
    Run `pnpm test:run -- tests/client-result.test.ts` — test MUST fail RED (run-foreground.ts still writes cancel on error).
  </action>
  <verify>
    <automated>pnpm --dir /Users/samuel/dev/tui-llm test:run -- tests/client-result.test.ts 2>&1 | grep -E 'FAIL|fail|Error' | head -10</automated>
  </verify>
  <done>
    The new test runs and fails with an assertion error (received kind is 'cancel', expected 'error'). All other tests in client-result.test.ts remain green (only the new test fails).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add confidence to NormalizedRequest + update run-foreground.ts + register bootstrap</name>
  <files>src/contracts/request.ts, src/client/run-foreground.ts, src/registry/bootstrap.ts</files>
  <read_first>
    - src/contracts/request.ts (full file — need current normalizedRequestSchema definition at lines 29–33; confirm IntentDecision already has confidence field)
    - src/client/run-foreground.ts (full file — locate line 96 NormalizedRequest construction, lines 170–174 inner .catch(), lines 178–184 outer catch)
    - src/registry/bootstrap.ts (full file — see current bootstrapBuiltins() and import list)
    - src/registry/provider-backends.ts (lines 1–12 — get ProviderBackendDescriptor shape and registerProviderBackend signature)
    - .planning/phases/03-claude-fast-path-and-ranked-suggestions/03-PATTERNS.md (sections on request.ts, run-foreground.ts, bootstrap.ts patterns)
  </read_first>
  <action>
    In src/contracts/request.ts:
    Extend normalizedRequestSchema to add confidence field:
      `export const normalizedRequestSchema = shellRequestSchema.extend({ intent: requestIntentSchema, confidence: z.number().min(0).max(1) });`
    The type alias `export type NormalizedRequest = z.infer<typeof normalizedRequestSchema>` needs no change — it auto-infers the new field.

    In src/client/run-foreground.ts:
    (a) Line ~96: change NormalizedRequest construction from
        `const normalized: NormalizedRequest = { ...request, intent: decision.intent };`
        to
        `const normalized: NormalizedRequest = { ...request, intent: decision.intent, confidence: decision.confidence };`
    (b) Inner .catch() (lines 170–174): add appendDebugLog with error message format per D-12. Keep `app.rerender(buildCandidateElement(null, true))` — this renders the error state in the Ink UI. No FIFO write in the inner catch (the user must press a key to dismiss; onCancel handler writes cancel).
    (c) Outer catch (lines 178–184): change the writeShellResult call from `{ kind: 'cancel' }` to `{ kind: 'error', message: \`QueQue: ${message} — press any key\` }`. Update the appendDebugLog event string from 'llm request failed; falling back to cancel' to 'llm request failed'.

    In src/registry/bootstrap.ts:
    (a) Add import: `import { registerProviderBackend } from './provider-backends.js';`
    (b) Inside bootstrapBuiltins(), after existing registrations, add:
        `registerProviderBackend({ id: 'claude', name: 'Claude (Anthropic)', description: 'Anthropic Claude adapter — default LLM backend' });`

    Run full test suite to verify GREEN:
    `pnpm test:run`
  </action>
  <verify>
    <automated>pnpm --dir /Users/samuel/dev/tui-llm test:run</automated>
  </verify>
  <done>
    pnpm test:run passes all tests (full suite green).
    grep -c "confidence: decision.confidence" /Users/samuel/dev/tui-llm/src/client/run-foreground.ts outputs 1.
    grep -c "kind: 'error'" /Users/samuel/dev/tui-llm/src/client/run-foreground.ts outputs at least 1.
    grep -c "QueQue:" /Users/samuel/dev/tui-llm/src/client/run-foreground.ts outputs at least 1.
    grep -c "registerProviderBackend" /Users/samuel/dev/tui-llm/src/registry/bootstrap.ts outputs 1.
    grep -c "confidence: z.number" /Users/samuel/dev/tui-llm/src/contracts/request.ts outputs 1.
    pnpm build exits 0 (no TypeScript errors on updated NormalizedRequest construction site).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fetchCandidates catch → writeShellResult | Error message string from provider exception flows into ShellResult; must not reach shell buffer |
| classifyIntent → NormalizedRequest.confidence | Synchronous score from intent classifier; no I/O, no injection surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | run-foreground outer catch error message | mitigate | error kind is never applied to shell buffer (ZSH widget no-ops on error kind, handled in Plan 03); message field is informational only |
| T-03-05 | Tampering | error message contains shell metacharacters | mitigate | The error ShellResult message field is never written to LBUFFER or RBUFFER; Plan 03 explicitly no-ops the ZSH buffer mutation for error kind |
| T-03-06 | Information Disclosure | confidence value logged | accept | confidence is a normalized float 0–1 from intent classification; no PII, no sensitive data |
</threat_model>

<verification>
After both tasks:
- pnpm test:run exits 0 (full suite green, including new error ShellResult test)
- pnpm build exits 0 (TypeScript happy with updated NormalizedRequest construction)
- NormalizedRequest has confidence field (z.number().min(0).max(1))
- run-foreground.ts outer catch writes error kind with QueQue: format
- bootstrapBuiltins registers claude provider backend descriptor
</verification>

<success_criteria>
- pnpm test:run exits 0
- pnpm build exits 0
- Error path writes { kind: 'error', message: 'QueQue: <reason> — press any key' } to FIFO
- NormalizedRequest.confidence flows from classifyIntent decision
- Claude backend descriptor registered in bootstrapBuiltins
</success_criteria>

<output>
After completion, create .planning/phases/03-claude-fast-path-and-ranked-suggestions/03-02-SUMMARY.md
</output>

---
phase: 06
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified:
  - README.md
  - CONTRIBUTING.md
  - docs/SYSTEM_DESIGN.md
  - docs/EXTENSIONS.md
  - docs/config.example.json
  - shell/zsh/queque.zsh
autonomous: true
requirements:
  - CMD-04

must_haves:
  truths:
    - "README documents privacy defaults, Zellij vs inline paths, and config.json merge behavior"
    - "docs/config.example.json matches QqConfig schema shape (privacy + safety top-level keys)"
    - "docs/EXTENSIONS.md describes Phase 7/8 expansion and registry APIs"
    - "docs/SYSTEM_DESIGN.md reflects Phases 3–6 reality (no placeholder language)"
    - "CONTRIBUTING.md lists daily-driver dev workflow and privacy env vars"
    - "QQ_PANE_WIDTH and QQ_PANE_HEIGHT implemented in queque.zsh (not doc-only)"
    - "No stale qq.zsh references in user-facing docs (use queque.zsh)"
  artifacts:
    - path: "docs/EXTENSIONS.md"
      provides: "Expansion path for plugins and providers"
      contains: "Phase 8"
    - path: "docs/config.example.json"
      provides: "Example privacy config for users"
      contains: "sensitivePathPatterns"
    - path: "docs/SYSTEM_DESIGN.md"
      provides: "Current architecture doc (renamed from SYSTEM_DESGN.md)"
      contains: "filterContextEnvelope"
  key_links:
    - from: "README.md"
      to: "docs/EXTENSIONS.md"
      via: "See docs/EXTENSIONS.md for expansion path"
      pattern: "EXTENSIONS.md"
    - from: "shell/zsh/queque.zsh"
      to: "QQ_PANE_WIDTH"
      via: "zellij run --width/--height"
      pattern: "QQ_PANE_WIDTH"
---

<objective>
Package QueQue for daily-driver usage: refresh stale docs, document privacy boundaries
and dual Zellij/inline paths, implement QQ_PANE env vars, and publish the expansion path
for Phase 7/8 work.

Purpose: Success criterion #2 (privacy documented) and #4 (ready for cross-OS zsh + plugins).

Output: Updated README, CONTRIBUTING, SYSTEM_DESIGN (renamed), config.example.json;
QQ_PANE_WIDTH/HEIGHT wired in queque.zsh.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-hardening-privacy-defaults-and-extension-seams/06-RESEARCH.md
@.planning/phases/06-hardening-privacy-defaults-and-extension-seams/06-REVIEWS.md
</context>

<artifacts_this_phase_produces>
- `docs/SYSTEM_DESIGN.md` (renamed from docs/SYSTEM_DESGN.md)
- `docs/config.example.json` with `{ "privacy": {...}, "safety": {...} }` shape
- `QQ_PANE_WIDTH` / `QQ_PANE_HEIGHT` env var support in shell/zsh/queque.zsh
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: README and config.example.json privacy documentation</name>
  <files>README.md, docs/config.example.json</files>
  <read_first>README.md, docs/config.example.json, docs/EXTENSIONS.md, src/shared/qq-config.ts</read_first>
  <behavior>
    - README has Privacy defaults section: insertion-only, what leaves machine, sensitive path stripping, debug log redaction, config.json merge
    - README documents Zellij recommended + inline fallback
    - config.example.json is strict JSON (no JSONC comments) with top-level privacy and safety keys
    - Field explanations live in README Configuration Reference table, not inside JSON
  </behavior>
  <action>
    Verify or add README section '## Privacy defaults' covering:
    - Insertion-only (CMD-04): writes to buffer, user presses Enter
    - What leaves machine: query, cwd, git metadata (names only), filename hints
    - What never sends: file contents unless QQ_ALLOW_FILE_READ=1 (gate only — no reads in v1)
    - Sensitive paths stripped via built-in + config merge; filter runs in pipeline and buildPrompt
    - Debug logging redaction unless QQ_DEBUG_VERBOSE=1
    - Malformed config.json ignored (fail-closed to built-in defaults)
    - Link to docs/config.example.json and docs/EXTENSIONS.md

    Write docs/config.example.json as strict JSON matching qq-config.ts schema:
    ```json
    {
      "privacy": {
        "sensitivePathPatterns": ["..."],
        "redactLogKeys": ["lbuffer", "rbuffer"],
        "allowFileRead": false
      },
      "safety": {
        "destructiveCommandPatterns": ["..."]
      }
    }
    ```
    Add README '## Configuration reference' table explaining each field — do NOT use _comment
    keys inside the JSON file.

    Fix any qq.zsh → queque.zsh references in README install instructions.

    Verify every config.example.json field exists in qqConfigFileSchema (src/shared/qq-config.ts).
  </action>
  <verify>
    <automated>grep -q 'Privacy defaults' README.md && jq -e '.privacy.sensitivePathPatterns' docs/config.example.json >/dev/null && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains heading '## Privacy defaults'
    - README.md contains 'insertion-only' or 'never auto-executes'
    - README.md links to docs/EXTENSIONS.md
    - docs/config.example.json has top-level "privacy" and "safety" keys
    - docs/config.example.json contains privacy.sensitivePathPatterns array
    - README.md contains Configuration reference table for config fields
    - grep 'qq\.zsh' README.md returns no matches (use queque.zsh)
  </acceptance_criteria>
  <done>User-facing privacy and config documentation complete in README + example config</done>
</task>

<task type="auto">
  <name>Task 2: Rename SYSTEM_DESIGN, refresh architecture and EXTENSIONS docs</name>
  <files>docs/SYSTEM_DESIGN.md, docs/EXTENSIONS.md</files>
  <read_first>docs/SYSTEM_DESGN.md, docs/EXTENSIONS.md, src/client/run-foreground.ts, shell/zsh/queque.zsh</read_first>
  <behavior>
    - docs/SYSTEM_DESGN.md renamed to docs/SYSTEM_DESIGN.md; all references updated
    - SYSTEM_DESIGN describes working Ink TUI, Claude provider, Zellij FIFO path — not placeholders
    - Request flow includes privacy filter: widget → client → gatherContext → filterContextEnvelope → resolveAdapter → TUI → result
    - EXTENSIONS documents registries, resolveAdapter, Phase 7/8 roadmap, privacy config table
    - No stale placeholder or pre-resolver language in EXTENSIONS.md
  </behavior>
  <action>
    Rename docs/SYSTEM_DESGN.md → docs/SYSTEM_DESIGN.md.
    Grep and update all references in README.md, CONTRIBUTING.md, docs/*.md.

    Update docs/SYSTEM_DESIGN.md:
    - Banner: 'Last updated Phase 6' with date
    - Replace placeholder language with Phase 3–6 reality
    - File paths: shell/zsh/queque.zsh (not qq.zsh)
    - Mermaid or ascii flow: widget → client → gatherContext → filterContextEnvelope →
      resolveAdapter → provider → TUI → FIFO/result
    - Document QQ_PANE_WIDTH/QQ_PANE_HEIGHT env vars

    Update docs/EXTENSIONS.md:
    - Registry table with getProviderAdapter, resolveAdapter flow
    - Phase 7 storage-hooks, Phase 8 provider adapters
    - Privacy config field table matching qq-config.ts
    - Remove any stale 'placeholder' or 'not yet wired' language that contradicts Phase 6 state
      (keep accurate Phase 8 deferrals for ollama/claude-cli adapters)
  </action>
  <verify>
    <automated>test ! -f docs/SYSTEM_DESGN.md && grep -q 'filterContextEnvelope' docs/SYSTEM_DESIGN.md && grep -q 'resolveAdapter' docs/EXTENSIONS.md && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - docs/SYSTEM_DESGN.md does not exist (renamed)
    - docs/SYSTEM_DESIGN.md does not contain 'still placeholders' or 'TUI.*placeholder'
    - docs/SYSTEM_DESIGN.md references queque.zsh not qq.zsh
    - docs/SYSTEM_DESIGN.md contains filterContextEnvelope in request flow
    - docs/EXTENSIONS.md contains 'resolveAdapter' and 'Phase 8'
    - docs/EXTENSIONS.md contains privacy config field table
    - grep -r 'SYSTEM_DESGN' README.md CONTRIBUTING.md docs/ returns no matches
  </acceptance_criteria>
  <done>Architecture docs reflect implemented system; typo filename fixed; expansion path documented</done>
</task>

<task type="auto">
  <name>Task 3: CONTRIBUTING workflow and QQ_PANE env vars in queque.zsh</name>
  <files>CONTRIBUTING.md, shell/zsh/queque.zsh, README.md</files>
  <read_first>CONTRIBUTING.md, shell/zsh/queque.zsh, README.md</read_first>
  <behavior>
    - CONTRIBUTING lists: build, test, QQ_DEV_ROOT, privacy env vars (QQ_ALLOW_FILE_READ, QQ_DEBUG_VERBOSE, QQ_CONFIG_FILE)
    - QQ_PANE_WIDTH (default 80) and QQ_PANE_HEIGHT (default 24) read in queque.zsh for zellij run
    - Env vars documented in README and CONTRIBUTING only after code implementation lands
  </behavior>
  <action>
    shell/zsh/queque.zsh: replace hardcoded --width 80 --height 24 with:
    - QQ_PANE_WIDTH=${QQ_PANE_WIDTH:-80}
    - QQ_PANE_HEIGHT=${QQ_PANE_HEIGHT:-24}
    - Pass to zellij run --width "$QQ_PANE_WIDTH" --height "$QQ_PANE_HEIGHT"

    Verify with: zellij run --help | grep -E 'width|height' (manual note in SUMMARY if flags differ)

    Add CONTRIBUTING section 'Daily driver / dev workflow' with:
    - pnpm build && pnpm test:run (or corepack pnpm if pnpm not on PATH)
    - export QQ_DEV_ROOT for local widget sourcing
    - Privacy env vars table: QQ_ALLOW_FILE_READ, QQ_DEBUG_VERBOSE, QQ_CONFIG_FILE
    - QQ_PANE_WIDTH, QQ_PANE_HEIGHT for Zellij pane sizing
    - Pointer to docs/config.example.json

    Add brief QQ_PANE note to README Zellij section.

    Fix CONTRIBUTING source path: shell/zsh/queque.zsh not qq.zsh.
  </action>
  <verify>
    <automated>grep -q 'QQ_PANE_WIDTH' shell/zsh/queque.zsh && grep -q 'QQ_CONFIG_FILE' CONTRIBUTING.md && grep -q 'test:run' CONTRIBUTING.md && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - shell/zsh/queque.zsh reads QQ_PANE_WIDTH and QQ_PANE_HEIGHT with defaults 80 and 24
    - shell/zsh/queque.zsh passes --width and --height to zellij run
    - CONTRIBUTING.md mentions QQ_ALLOW_FILE_READ and QQ_DEBUG_VERBOSE
    - CONTRIBUTING.md documents QQ_PANE_WIDTH and QQ_PANE_HEIGHT
    - CONTRIBUTING.md contains pnpm test:run in dev workflow
    - grep 'source.*qq\.zsh' CONTRIBUTING.md returns no matches (use queque.zsh)
  </acceptance_criteria>
  <done>Contributor docs complete; Zellij pane env vars implemented and documented</done>
</task>

</tasks>

<verification>
Run after all tasks:

  grep -r 'qq\.zsh\|SYSTEM_DESGN\|placeholder\|not implemented' README.md CONTRIBUTING.md docs/ --include='*.md' || true
  grep -q 'QQ_PANE_WIDTH' shell/zsh/queque.zsh

Expected: no qq.zsh or SYSTEM_DESGN references; no placeholder language in SYSTEM_DESIGN;
QQ_PANE env vars wired in shell.
</verification>

<success_criteria>
- Privacy defaults documented for end users with config reference table
- config.example.json matches QqConfig schema (strict JSON)
- Architecture docs match implemented codebase (SYSTEM_DESIGN renamed)
- QQ_PANE_WIDTH/HEIGHT implemented — not doc-only
- Expansion path clear for Phase 7/8
- Doc drift (qq.zsh, SYSTEM_DESGN typo) eliminated
</success_criteria>

<output>
After completion, create .planning/phases/06-hardening-privacy-defaults-and-extension-seams/06-03-SUMMARY.md
</output>

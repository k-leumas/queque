---
phase: 06
plan: 02
type: execute
wave: 1
depends_on:
  - 06-01
files_modified:
  - src/registry/provider-backends.ts
  - src/registry/bootstrap.ts
  - src/providers/resolver.ts
  - src/providers/claude.ts
  - src/client/run-foreground.ts
  - src/cli/commands/init.ts
  - tests/provider-resolver.test.ts
  - tests/registry-bootstrap.test.ts
  - tests/client-result.test.ts
  - tests/claude-provider.test.ts
autonomous: true
requirements:
  - CMD-04
  - EXT-01

must_haves:
  truths:
    - "run-foreground.ts resolves LLM adapter via resolveAdapter(detectProvider()) — no direct claude.ts import"
    - "bootstrap registers claudeAdapter instance in provider-backends registry"
    - "resolveAdapter throws clear user-facing error when bootstrap not run or adapter missing"
    - "resolveAdapter throws clear error for ollama/claude-cli/openai-key until Phase 8"
    - "unsupported provider errors become ShellResult kind error — no partial result files"
    - "buildPrompt applies filterContextEnvelope as defense-in-depth before prompt JSON"
    - "init.ts validates shells via listShellAdapters() after bootstrapBuiltins()"
    - "bootstrapBuiltins is idempotent (safe to call twice)"
    - "pnpm test:run exits 0"
  artifacts:
    - path: "src/providers/resolver.ts"
      provides: "Maps DetectedProvider to registered LLMAdapter"
      contains: "resolveAdapter"
    - path: "src/registry/provider-backends.ts"
      provides: "Provider registry with adapter instances"
      contains: "getProviderAdapter"
  key_links:
    - from: "src/client/run-foreground.ts"
      to: "src/providers/resolver.ts"
      via: "resolveAdapter(detected)"
      pattern: "resolveAdapter"
    - from: "src/providers/claude.ts"
      to: "src/shared/privacy-filter.ts"
      via: "filterContextEnvelope in buildPrompt"
      pattern: "filterContextEnvelope"
    - from: "src/registry/bootstrap.ts"
      to: "src/providers/claude.ts"
      via: "registerProviderBackend with adapter: claudeAdapter"
      pattern: "adapter: claudeAdapter"
---

<objective>
Verify and harden registry-backed provider resolution. Close any remaining bypass where
run-foreground.ts imports fetchCandidates directly. Add defense-in-depth privacy filter in
buildPrompt. Ensure unsupported-provider and missing-bootstrap errors are user-visible.

Purpose: Make extension seams real for Phase 8 multi-provider work and satisfy EXT-01.

Output: Registry-backed provider resolution with integration tests; buildPrompt filters
envelope; init uses listShellAdapters(); error paths tested.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-hardening-privacy-defaults-and-extension-seams/06-RESEARCH.md
@.planning/phases/06-hardening-privacy-defaults-and-extension-seams/06-PATTERNS.md
@.planning/phases/06-hardening-privacy-defaults-and-extension-seams/06-REVIEWS.md
</context>

<artifacts_this_phase_produces>
- `resolveAdapter(detected: DetectedProvider): LLMAdapter`
- `getProviderAdapter(id: string): LLMAdapter | undefined`
- `clearProviderBackends(): void` (test isolation — already exists, verify used in tests)
- `buildPrompt` defense-in-depth via `filterContextEnvelope(envelope)` before JSON.stringify
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Provider registry, resolveAdapter guards, and bootstrap idempotency</name>
  <files>src/registry/provider-backends.ts, src/registry/bootstrap.ts, src/providers/resolver.ts, tests/provider-resolver.test.ts, tests/registry-bootstrap.test.ts</files>
  <read_first>src/registry/provider-backends.ts, src/registry/bootstrap.ts, src/providers/resolver.ts, src/providers/claude.ts, tests/provider-resolver.test.ts, tests/registry-bootstrap.test.ts</read_first>
  <behavior>
    - RegisteredProviderBackend holds { id, name, description, adapter: LLMAdapter }
    - getProviderAdapter('claude') returns claudeAdapter after bootstrapBuiltins()
    - resolveAdapter({ kind: 'anthropic-key' }) returns claude adapter when bootstrapped
    - resolveAdapter before bootstrap throws: "was bootstrapBuiltins() called?" — not undefined deref
    - resolveAdapter({ kind: 'ollama' }) throws user-facing Phase 8 message
    - resolveAdapter({ kind: 'none' }) throws detected.message
    - bootstrapBuiltins() safe to call twice (idempotent — no duplicate registration throw)
  </behavior>
  <action>
    Verify provider-backends.ts uses RegisteredProviderBackend with adapter: LLMAdapter.
    Verify getProviderAdapter(id) returns registry.get(id)?.adapter.
    Verify clearProviderBackends() exists for test isolation.

    resolver.ts: when getProviderAdapter('claude') returns undefined, throw:
    'QueQue: Claude provider is not registered — was bootstrapBuiltins() called?'
    (Update message if different but must be user-displayable, not TypeError).

    bootstrap.ts: make registration idempotent — if 'claude' already registered, skip or
    replace without throwing. Add comment that main.ts also calls bootstrapBuiltins();
    init.ts call is defensive belt-and-suspenders.

    tests/provider-resolver.test.ts:
    - clearProviderBackends() in beforeEach
    - resolveAdapter({ kind: 'anthropic-key' }) after bootstrap returns adapter
    - resolveAdapter({ kind: 'anthropic-key' }) before bootstrap throws clear message
    - resolveAdapter({ kind: 'ollama' }) throws Phase 8 message
    - resolveAdapter({ kind: 'none', message: '...' }) throws message

    tests/registry-bootstrap.test.ts:
    - bootstrapBuiltins() twice does not throw
    - getProviderAdapter('claude')?.fetchCandidates is function after bootstrap

    Export resolveAdapter from src/providers/index.ts if not already.
    Run pnpm tsc --noEmit to confirm no circular imports between bootstrap/resolver/claude.
  </action>
  <verify>
    <automated>pnpm test:run -- tests/provider-resolver.test.ts tests/registry-bootstrap.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/registry/provider-backends.ts exports getProviderAdapter and RegisteredProviderBackend
    - src/providers/resolver.ts exports resolveAdapter
    - resolveAdapter throws user-facing error (not TypeError) when adapter missing
    - tests/provider-resolver.test.ts covers pre-bootstrap throw
    - tests/registry-bootstrap.test.ts covers bootstrapBuiltins called twice
    - pnpm test:run -- tests/provider-resolver.test.ts exits 0
  </acceptance_criteria>
  <done>Provider registry stores adapter instances; resolveAdapter guarded and idempotent bootstrap verified</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire run-foreground, error paths, and test mock audit</name>
  <files>src/client/run-foreground.ts, tests/client-result.test.ts</files>
  <read_first>src/client/run-foreground.ts, tests/client-result.test.ts, tests/provider-resolver.test.ts</read_first>
  <behavior>
    - runForegroundClient calls resolveAdapter(await detectProvider()) then adapter.fetchCandidates(envelope)
    - resolveAdapter throw writes ShellResult { kind: 'error', message } — no partial result file
    - client-result.test.ts mocks ../providers/resolver.js not ../providers/claude.js
    - No other test files mock claude.js for run-foreground path (audit grep)
  </behavior>
  <action>
    Verify run-foreground.ts imports resolveAdapter, not fetchCandidates from claude.ts.
    If direct claude import remains, remove it.

    Ensure catch block around resolveAdapter/fetchCandidates writes kind:'error' ShellResult
    with user-facing message (not raw stack trace).

    Update tests/client-result.test.ts:
    - vi.mock('../providers/resolver.js') with resolveAdapter returning mock adapter
    - Add test: resolveAdapter throws → result kind is 'error' with message substring
    - Add test: ollama-kind resolveAdapter throw → error ShellResult, no candidates written

    Audit: grep -r "vi.mock.*claude" tests/ — update any test that mocks claude.js for
    client path to mock resolver.js instead. claude-provider.test.ts may keep direct
    claude import (tests the adapter itself).

    Acceptance for run-foreground grep belongs HERE (Task 2), not Task 1.
  </action>
  <verify>
    <automated>pnpm test:run -- tests/client-result.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/client/run-foreground.ts imports resolveAdapter from '../providers/resolver.js'
    - grep "fetchCandidates" src/client/run-foreground.ts shows only adapter.fetchCandidates call
    - grep "from '../providers/claude.js'" src/client/run-foreground.ts returns no matches
    - tests/client-result.test.ts mocks resolver.js resolveAdapter
    - tests/client-result.test.ts includes resolveAdapter throw → error ShellResult test
    - pnpm test:run -- tests/client-result.test.ts exits 0
  </acceptance_criteria>
  <done>Client uses registry-backed adapter resolution; error paths and mocks verified</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: init.ts alignment, buildPrompt defense-in-depth, filesystem extras</name>
  <files>src/cli/commands/init.ts, src/providers/claude.ts, tests/claude-provider.test.ts</files>
  <read_first>src/cli/commands/init.ts, src/registry/shell-adapters.ts, src/providers/claude.ts, src/shared/privacy-filter.ts, tests/claude-provider.test.ts</read_first>
  <behavior>
    - initCommand calls bootstrapBuiltins() then validates shell against listShellAdapters()
    - buildPrompt calls filterContextEnvelope(envelope) before extracting git/filesystem chunks
    - buildPrompt JSON includes filesystem chunk when envelope has kind:'filesystem' extra
    - Prompt never includes sensitive git paths even if pipeline filter missed one (defense-in-depth)
  </behavior>
  <action>
    init.ts: ensure bootstrapBuiltins() runs before supportedShells = listShellAdapters().map(...).
    Add comment: defensive init — main.ts also bootstraps.

    claude.ts buildPrompt:
    - Import filterContextEnvelope from '../shared/privacy-filter.js'
    - First line: const filtered = filterContextEnvelope(envelope)
    - Use filtered.extras for gitChunk and filesystemChunk extraction
    - Include filesystem: { cwd, apparentFilename } in Shell context JSON when present

    tests/claude-provider.test.ts:
    - Test envelope with filesystem extra → prompt JSON contains apparentFilename
    - Test envelope with git changedFiles ['.env', 'src/index.ts'] → prompt JSON excludes .env path
      (proves buildPrompt defense-in-depth)
  </action>
  <verify>
    <automated>pnpm test:run -- tests/claude-provider.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/cli/commands/init.ts calls bootstrapBuiltins() before listShellAdapters()
    - src/providers/claude.ts buildPrompt calls filterContextEnvelope before chunk extraction
    - src/providers/claude.ts buildPrompt includes filesystem payload when chunk present
    - tests/claude-provider.test.ts asserts apparentFilename in prompt
    - tests/claude-provider.test.ts asserts .env path stripped from prompt JSON
    - pnpm test:run -- tests/claude-provider.test.ts exits 0
  </acceptance_criteria>
  <done>init uses shell registry; buildPrompt filters envelope and includes filesystem metadata</done>
</task>

</tasks>

<verification>
Run after all tasks:

  pnpm test:run

Expected: provider-resolver, registry-bootstrap, client-result, claude-provider all green.
grep confirms no direct fetchCandidates import in run-foreground.ts.
</verification>

<success_criteria>
- Single resolution path: detectProvider → resolveAdapter → fetchCandidates
- Registry stores callable adapter instances with test isolation
- buildPrompt applies filterContextEnvelope (defense-in-depth)
- Unsupported provider and missing-bootstrap errors are user-visible ShellResult errors
- init.ts aligned with shell-adapters registry
- pnpm test:run exits 0
</success_criteria>

<output>
After completion, create .planning/phases/06-hardening-privacy-defaults-and-extension-seams/06-02-SUMMARY.md
</output>

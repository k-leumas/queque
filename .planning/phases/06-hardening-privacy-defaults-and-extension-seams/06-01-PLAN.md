---
phase: 06
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/shared/privacy-filter.ts
  - src/shared/qq-config.ts
  - src/context/pipeline.ts
  - src/shared/debug-log.ts
  - src/client/run-foreground.ts
  - src/ui/CandidateSelect.tsx
  - tests/privacy-filter.test.ts
  - tests/debug-log.test.ts
  - tests/context-pipeline.test.ts
  - tests/candidate-select.test.tsx
  - tests/zsh-widget.test.ts
autonomous: true
requirements:
  - CMD-04

must_haves:
  truths:
    - "filterContextEnvelope strips sensitive paths from git.changedFiles before API calls"
    - "pipeline.ts calls filterContextEnvelope after provider gather loop"
    - "appendDebugLog calls redactForLog before JSON.stringify"
    - "isFileReadAllowed returns false unless QQ_ALLOW_FILE_READ=1 or config privacy.allowFileRead"
    - "QQ_ALLOW_FILE_READ gate exists but no provider reads file bytes in Phase 6"
    - "CandidateSelect shows destructive warning below selected candidate (warn-only)"
    - "zsh widget never evals or executes selected commands (CMD-04)"
    - "pnpm test:run exits 0"
  artifacts:
    - path: "src/shared/privacy-filter.ts"
      provides: "Privacy filter layer for envelope and logs"
      contains: "filterContextEnvelope"
    - path: "tests/debug-log.test.ts"
      provides: "Debug log redaction regression tests"
      contains: "redactForLog"
    - path: "tests/context-pipeline.test.ts"
      provides: "Pipeline integration test for envelope filtering"
      contains: "filterContextEnvelope"
  key_links:
    - from: "src/context/pipeline.ts"
      to: "src/shared/privacy-filter.ts"
      via: "filterContextEnvelope after gather"
      pattern: "filterContextEnvelope"
    - from: "src/shared/debug-log.ts"
      to: "src/shared/privacy-filter.ts"
      via: "redactForLog in formatDetails"
      pattern: "redactForLog\\(details\\)"
---

<objective>
Verify and harden privacy-aware context filtering, debug-log redaction, and CMD-04
safety guards. QueQue must enforce privacy-forward defaults operationally — not just in
schema comments — and remain insertion-only with auditable integration tests.

Purpose: Close Phase 2 D-05/D-06 gaps and make CMD-04 fail-closed and testable.

Output: Privacy filter module verified; pipeline wiring tested end-to-end; destructive-command
warn-only UI badge with explicit placement; CMD-04 zsh tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-hardening-privacy-defaults-and-extension-seams/06-RESEARCH.md
@.planning/phases/06-hardening-privacy-defaults-and-extension-seams/06-PATTERNS.md
@.planning/phases/06-hardening-privacy-defaults-and-extension-seams/06-REVIEWS.md
@.planning/phases/02-intent-router-and-context-pipeline/02-CONTEXT.md
</context>

<artifacts_this_phase_produces>
- `isSensitivePath(path: string): boolean`
- `filterContextEnvelope(envelope: ContextEnvelope): ContextEnvelope`
- `redactForLog(details: unknown): unknown`
- `isFileReadAllowed(): boolean`
- `isDestructiveCommand(command: string): boolean`
- `loadQqConfig()` malformed-config fail-closed behavior (documented + tested)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Privacy filter module verification and test hardening</name>
  <files>src/shared/privacy-filter.ts, src/shared/qq-config.ts, tests/privacy-filter.test.ts</files>
  <read_first>src/shared/privacy-filter.ts, src/shared/qq-config.ts, src/contracts/request.ts, tests/privacy-filter.test.ts</read_first>
  <behavior>
    - filterContextEnvelope removes .env, credentials, .pem, id_rsa, .ssh/, secrets paths from git.changedFiles
    - isFileReadAllowed defaults false; QQ_ALLOW_FILE_READ=1 or config privacy.allowFileRead enables
    - redactForLog replaces lbuffer/rbuffer with [redacted:Nchars] unless QQ_DEBUG_VERBOSE=1
    - QQ_DEBUG_VERBOSE=1 still redacts obvious secret-shaped values (API keys) in nested objects
    - isDestructiveCommand matches rm -rf, sudo, chmod -R, curl | sh patterns (warn-only, not block)
    - User config at ~/.config/qq/config.json merges patterns onto built-in defaults (QQ_CONFIG_FILE override)
    - Malformed config.json fails closed: loadQqConfig returns built-in defaults and does not throw
    - QQ_ALLOW_FILE_READ gate must not cause any provider to read file bytes (gate only, no reads)
  </behavior>
  <action>
    Verify src/shared/privacy-filter.ts exports: isSensitivePath, filterContextEnvelope,
    redactForLog, isFileReadAllowed, isDestructiveCommand.

    If implementation exists from quick task 20260617-privacy-config, verify acceptance criteria
    and extend tests only for gaps — do not rewrite working code.

    Extend tests/privacy-filter.test.ts:
    - Sensitive paths: .env, .env.local, credentials.json, foo.pem, .ssh/id_rsa, secrets/foo
    - Safe path src/index.ts passes through unchanged
    - redactForLog strips lbuffer; nested object with lbuffer inside request also redacted
    - redactForLog handles arrays and error-shaped objects
    - QQ_DEBUG_VERBOSE=1 preserves structure but still redacts ANTHROPIC_API_KEY-shaped strings
    - config merge: user sensitivePathPatterns added to defaults, not replacing them
    - isFileReadAllowed: unset env + allowFileRead false → false; config allowFileRead true → true
    - isDestructiveCommand: true for 'rm -rf /tmp', false for 'git status', quoted 'rm' in string handled
    - loadQqConfig with invalid JSON returns defaults (fail-closed)

    Grep src/context/providers/ and src/providers/ — confirm no file byte reads gated only by
    isFileReadAllowed (gate is forward-looking for Phase 7).
  </action>
  <verify>
    <automated>pnpm test:run -- tests/privacy-filter.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/privacy-filter.test.ts contains describe blocks for filterContextEnvelope and redactForLog
    - tests/privacy-filter.test.ts includes isFileReadAllowed test with config allowFileRead true
    - tests/privacy-filter.test.ts includes malformed config fail-closed test
    - pnpm test:run -- tests/privacy-filter.test.ts exits 0
    - src/shared/privacy-filter.ts exports filterContextEnvelope and redactForLog
    - grep -r 'readFileSync\|readFile' src/context/providers/ returns no file-content reads for query context
  </acceptance_criteria>
  <done>Privacy filter module and tests green; sensitive paths stripped; config fail-closed verified</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Debug log redaction and pipeline integration tests</name>
  <files>src/shared/debug-log.ts, src/context/pipeline.ts, tests/debug-log.test.ts, tests/context-pipeline.test.ts</files>
  <read_first>src/shared/debug-log.ts, src/context/pipeline.ts, src/client/run-foreground.ts, tests/context-pipeline.test.ts</read_first>
  <behavior>
    - appendDebugLog passes details through redactForLog before JSON.stringify
    - pipeline logs queryLength not full lbuffer in 'pipeline start' message
    - run-foreground logs redacted request summary not full lbuffer/rbuffer
    - gatherContext return value passes through filterContextEnvelope (integration, not module-only)
  </behavior>
  <action>
    Verify or create tests/debug-log.test.ts:
    - Mock fsp.appendFile; call appendDebugLog with { lbuffer: 'secret query', cwd: '/tmp' }
    - Assert written line contains [redacted:12chars] or similar, not 'secret query'
    - With QQ_DEBUG_VERBOSE=1, assert lbuffer appears in log line

    Verify src/shared/debug-log.ts calls redactForLog(details) in formatDetails path.
    Verify src/context/pipeline.ts logs queryLength in pipeline start, not lbuffer string.
    Verify src/client/run-foreground.ts does not pass raw request with lbuffer to appendDebugLog.

    Extend tests/context-pipeline.test.ts:
    - Mock a git provider returning changedFiles including '.env' and 'src/index.ts'
    - Assert gatherContext() envelope excludes '.env' but retains 'src/index.ts'
    - This proves pipeline wiring, not just filterContextEnvelope in isolation
  </action>
  <verify>
    <automated>pnpm test:run -- tests/debug-log.test.ts tests/context-pipeline.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/debug-log.test.ts exists and pnpm test:run -- tests/debug-log.test.ts exits 0
    - src/shared/debug-log.ts calls redactForLog(details) — not merely defines or imports it
    - src/context/pipeline.ts contains filterContextEnvelope( in gatherContext return path
    - src/context/pipeline.ts logs queryLength in pipeline start, not lbuffer string
    - tests/context-pipeline.test.ts asserts sensitive git path removed from gatherContext output
    - grep -n 'lbuffer' src/context/pipeline.ts shows no appendDebugLog with raw request.lbuffer
  </acceptance_criteria>
  <done>Debug logs redact buffer text by default; pipeline privacy hook integration-tested</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CMD-04 safety guards — zsh insertion-only tests and destructive UI warning</name>
  <files>tests/zsh-widget.test.ts, src/ui/CandidateSelect.tsx, tests/candidate-select.test.tsx</files>
  <read_first>shell/zsh/queque.zsh, tests/zsh-widget.test.ts, src/ui/CandidateSelect.tsx</read_first>
  <behavior>
    - Widget sets LBUFFER/RBUFFER on replace-buffer; never evals, zle -s, or zle accept-line on candidate
    - CandidateSelect shows yellow 'review carefully — this command may be destructive' below the
      selected candidate detail area when command matches isDestructiveCommand
    - Warning is display-only; Enter still selects the command (user control preserved)
  </behavior>
  <action>
    Extend tests/zsh-widget.test.ts describe 'CMD-04: insertion-only shell contract':
    - grep widget for eval|zle -s — expect no matches
    - Test replace-buffer sets LBUFFER to command text without accept-line side effects

    Extend tests/candidate-select.test.tsx:
    - When selected candidate command is 'rm -rf /tmp/foo', assert destructive warning text renders
      in the detail/footer area below the selected item
    - When command is 'git status', assert warning text absent
    - Assert onSelect still fires for destructive command (warn-only, not blocked)

    CandidateSelect.tsx must import isDestructiveCommand from privacy-filter and render
    warn-only badge below selected candidate detail — not blocking the list or onSelect.
  </action>
  <verify>
    <automated>pnpm test:run -- tests/zsh-widget.test.ts tests/candidate-select.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/zsh-widget.test.ts contains describe 'CMD-04: insertion-only shell contract'
    - tests/candidate-select.test.tsx asserts destructive warning for rm -rf pattern
    - tests/candidate-select.test.tsx asserts onSelect not blocked for destructive command
    - src/ui/CandidateSelect.tsx contains 'review carefully — this command may be destructive'
    - grep -E '\\beval\\b|zle -s' shell/zsh/queque.zsh returns no matches
  </acceptance_criteria>
  <done>CMD-04 auditable via tests; destructive commands show warn-only UI badge below selection</done>
</task>

</tasks>

<verification>
Run after all tasks:

  pnpm test:run

Expected: full suite green including privacy-filter, debug-log, context-pipeline integration,
zsh-widget CMD-04, and candidate-select destructive warning tests.
</verification>

<success_criteria>
- Privacy filter operational in pipeline before API calls (integration-tested)
- Debug logs redact buffer text by default
- CMD-04 insertion-only verified in zsh tests
- Destructive command warning visible below selected candidate (warn-only)
- QQ_ALLOW_FILE_READ gate present with no file byte reads in Phase 6
- pnpm test:run exits 0
</success_criteria>

<output>
After completion, create .planning/phases/06-hardening-privacy-defaults-and-extension-seams/06-01-SUMMARY.md
</output>

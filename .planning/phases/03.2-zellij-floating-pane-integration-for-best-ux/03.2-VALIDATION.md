---
phase: 3.2
slug: zellij-floating-pane-integration-for-best-ux
status: draft
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-14
---

# Phase 3.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest 4.0.4 |
| **Config file** | `vitest.config.ts` |
| **Quick run command** | `pnpm test:run` |
| **Full suite command** | `pnpm test:run` |
| **Estimated runtime** | ~10 seconds |

---

## Sampling Rate

- **After every task commit:** Run `pnpm test:run`
- **After every plan wave:** Run `pnpm test:run`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 15 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 3.2-01-01 | 01 | 0 | D-01 | T-3.2-01 | Non-Zellij exits without spawning processes | unit | `pnpm test:run -- zsh-widget` | ✅ extend existing | ⬜ pending |
| 3.2-01-02 | 01 | 1 | D-03,D-05 | T-3.2-02 | FIFO path not world-writable | unit | `pnpm test:run -- zsh-widget` | ✅ extend existing | ⬜ pending |
| 3.2-01-03 | 01 | 1 | D-04 | T-3.2-03 | FIFO write via isFIFO path (no rename) | unit | `pnpm test:run -- client-result` | ✅ extend existing | ⬜ pending |
| 3.2-01-04 | 01 | 1 | D-08 | — | ZELLIJ branch skips ttyHandle open | unit | `pnpm test:run -- client-result` | ✅ extend existing | ⬜ pending |
| 3.2-01-05 | 01 | 2 | TUI-01 | — | Full round-trip: pane opens, select, buffer updated | manual | — | manual only | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/zsh-widget.test.ts` — add describe blocks: "Zellij detection: exits with message when $ZELLIJ is unset" and "FIFO round-trip: widget creates FIFO, receives JSON, applies result"
- [ ] `tests/client-result.test.ts` — update `node:fs/promises` mock to cover `stat: vi.fn().mockResolvedValue({ isFIFO: () => false })` for existing tests; add FIFO-path test cases

*Existing infrastructure covers all phase requirements — no new test files needed.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Floating pane opens at 80×24 in Zellij | TUI-01, D-10 | Requires live Zellij session | Run `??` in zsh inside Zellij; verify floating pane appears at correct size |
| Full round-trip: `??` → select → buffer | D-05,D-06 | Requires live Zellij + ZSH environment | Type query, select candidate, verify it lands in shell buffer |
| Non-Zellij error message | D-01 | Requires running outside Zellij | Run `qq client ...` without `$ZELLIJ` set; verify message and exit code |
| FIFO cleanup on cancel | D-05 | Shell state | Cancel with Esc, verify FIFO file removed | 

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 15s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-05-14

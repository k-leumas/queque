---
phase: "03.2"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/client-result.test.ts
  - tests/zsh-widget.test.ts
autonomous: true
requirements:
  - TUI-01
  - SHL-04

must_haves:
  truths:
    - "pnpm test:run exits 0 with updated mocks in place (stat mock does not break existing writeShellResult tests)"
    - "client-result.test.ts mock for node:fs/promises includes a stat function returning isFIFO() => false by default"
    - "client-result.test.ts includes a test asserting writeShellResult uses fsp.writeFile directly when isFIFO() => true"
    - "zsh-widget.test.ts includes a test asserting Zellij detection: script exits non-zero and contains expected message when ZELLIJ is unset"
    - "zsh-widget.test.ts includes a test asserting the widget uses mkfifo and zellij run (grep-based static checks)"
  artifacts:
    - path: "tests/client-result.test.ts"
      provides: "Updated stat mock + FIFO-path test cases"
      contains: "stat: vi.fn"
    - path: "tests/zsh-widget.test.ts"
      provides: "Zellij detection test + static content assertions"
      contains: "Zellij detection"
  key_links:
    - from: "tests/client-result.test.ts"
      to: "src/client/result-writer.ts"
      via: "dynamic import"
      pattern: "import.*result-writer"
    - from: "tests/zsh-widget.test.ts"
      to: "shell/zsh/qq.zsh"
      via: "spawnSync zsh -f -c source"
      pattern: "spawnSync.*zsh"
---

<objective>
Update both existing test files so that they cover Phase 3.2 behaviors before the production files are modified. This is the Wave 1 foundation: the tests go RED first (or are neutral stubs) so that Wave 2 implementation has immediate automated feedback.

Purpose: Establish the test contracts for (a) FIFO-aware write in result-writer.ts, (b) $ZELLIJ detection and skip in run-foreground.ts, and (c) Zellij detection guard in the zsh widget. All tests must use the correct mock shape so existing passing tests remain green after this plan lands.

Output: Two modified test files. No production code changes in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.2-zellij-floating-pane-integration-for-best-ux/03.2-CONTEXT.md
@.planning/phases/03.2-zellij-floating-pane-integration-for-best-ux/03.2-RESEARCH.md
@.planning/phases/03.2-zellij-floating-pane-integration-for-best-ux/03.2-PATTERNS.md
</context>

<interfaces>
<!-- Current mock shape in tests/client-result.test.ts (lines 34-47). -->
<!-- The existing vi.mock('node:fs/promises') spreads 'original' but only overrides 'open'. -->
<!-- Phase 3.2 requires adding 'stat' to that mock so result-writer.ts can call fsp.stat(). -->

From tests/client-result.test.ts (lines 34-47 — current mock shape):
The mock overrides only fsp.open. The stat function is not mocked, so it falls through to
the real fs/promises module. After this plan, stat must return { isFIFO: () => false }
by default so existing writeShellResult tests continue to use the atomic-rename path.

From tests/zsh-widget.test.ts (lines 35-46 — runZsh helper):
function runZsh(script: string): { stdout: string; stderr: string; status: number }
The helper passes env: { ...process.env, PATH: process.env.PATH }.
Any inherited ZELLIJ env var will propagate to the child process unless explicitly removed.
For the non-Zellij detection test, create a variant that strips ZELLIJ from the environment.

Key ZELLIJ env behavior (RESEARCH.md Finding 2):
- Inside Zellij: ZELLIJ=0 (string "0", not "1" or "true")
- Outside Zellij: ZELLIJ is unset (undefined)
- Correct zsh check: [[ -z "$ZELLIJ" ]] → exits with message
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend client-result.test.ts — stat mock + FIFO write tests</name>
  <files>tests/client-result.test.ts</files>
  <read_first>
    - tests/client-result.test.ts — read the entire file; understand current mock shape (vi.mock node:fs/promises), existing writeShellResult describe block, existing runForegroundClient describe block
    - src/client/result-writer.ts — read the current atomic-rename implementation to understand what the new FIFO branch must replace
    - .planning/phases/03.2-zellij-floating-pane-integration-for-best-ux/03.2-PATTERNS.md — §"tests/client-result.test.ts — mock extension for fsp.stat" section
  </read_first>
  <behavior>
    - Test: writeShellResult calls fsp.writeFile directly (not fsp.rename) when stat().isFIFO() returns true
    - Test: writeShellResult still uses fsp.rename (atomic path) when stat().isFIFO() returns false
    - Test: existing "writes {kind: cancel}" test continues to pass (stat mock default returns isFIFO: () => false)
    - Test: process.env['ZELLIJ'] !== undefined branch in runForegroundClient skips fsp.open('/dev/tty') call
  </behavior>
  <action>
    Modify tests/client-result.test.ts with two targeted changes:

    CHANGE 1 — Extend the existing vi.mock('node:fs/promises') block (lines 34-47) to add a stat mock alongside the existing open mock. The stat mock must:
    - Default to returning { isFIFO: () => false } (regular file path — existing tests stay green)
    - Be a vi.fn() so individual tests can override it with mockResolvedValueOnce({ isFIFO: () => true })

    The updated mock object inside vi.mock must include:
      stat: vi.fn().mockResolvedValue({ isFIFO: () => false })

    CHANGE 2 — Add a new describe block named 'writeShellResult: FIFO-aware write' after the existing 'writeShellResult' describe block. This describe block must contain two it() tests:

    Test A: "writes directly to FIFO path without rename when isFIFO() is true"
      - Override the stat mock for this test only: vi.mocked(fsp.stat).mockResolvedValueOnce({ isFIFO: () => true } as ReturnType<typeof fsp.stat>)
      - Call writeShellResult with a fake FIFO path string (does not need to be a real FIFO — the mock intercepts stat)
      - Wait for it to resolve
      - Assert that vi.mocked(fsp.rename) was NOT called (use expect(vi.mocked(fsp.rename)).not.toHaveBeenCalled())
      - Assert that vi.mocked(fsp.writeFile) WAS called with the original resultFile path as the first argument (not a .tmp path)

      This test requires fsp.rename and fsp.writeFile to also be mocked as vi.fn(). Add them to the mock object:
        writeFile: vi.fn().mockResolvedValue(undefined),
        rename: vi.fn().mockResolvedValue(undefined),

      Note: The existing writeShellResult tests use fs.readFileSync to verify the file was actually written. Those tests use the real fs module (not the mock), so they will break if writeFile is globally mocked. Solution: scope the FIFO branch tests to use a beforeEach/afterEach that resets mocks, OR create a separate describe block that resets global mocks. Preferred approach: move the FIFO-branch tests into their own describe block that calls vi.resetAllMocks() in beforeEach and re-applies the needed mocks per-test.

    Test B: "uses atomic rename (tmp + rename) for regular file paths when isFIFO() is false"
      - Stat mock remains at default (isFIFO: () => false)
      - The test verifies that fsp.rename IS called with a .tmp intermediate path
      - Assert: vi.mocked(fsp.rename).mock.calls[0][0] ends with '.tmp' and vi.mocked(fsp.rename).mock.calls[0][1] equals the resultFile path

    CHANGE 3 — Add a new describe block named 'runForegroundClient: Zellij branch skips /dev/tty open' containing one test:

    Test C: "does not call fsp.open('/dev/tty') when process.env.ZELLIJ is defined"
      - Set process.env['ZELLIJ'] = '0' before calling runForegroundClient
      - Restore process.env['ZELLIJ'] to its original value in afterEach (use delete operator if it was undefined)
      - Call runForegroundClient with resultMode: 'cancel' and a valid requestFile
      - Assert that fsp.open was NOT called with '/dev/tty' as its first argument
      - Assert that writeShellResult WAS called (the cancel path still writes a result)

    Import fsp at the top of the test file for spy access:
      import * as fsp from 'node:fs/promises';

    The import block at lines 1-7 already includes the necessary imports (spawnSync, fs, os, path, dirname, fileURLToPath, vitest). Add the fsp import after line 6.
  </action>
  <verify>
    <automated>cd /Users/samuel/dev/tui-llm && pnpm test:run -- client-result 2>&1 | tail -30</automated>
  </verify>
  <done>
    pnpm test:run -- client-result exits 0. The new FIFO mock tests pass (Test A: rename not called, writeFile called with original path; Test B: rename called with .tmp path; Test C: fsp.open('/dev/tty') not called when ZELLIJ=0). Existing writeShellResult tests still pass with real file I/O.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend zsh-widget.test.ts — Zellij detection + static content assertions</name>
  <files>tests/zsh-widget.test.ts</files>
  <read_first>
    - tests/zsh-widget.test.ts — read the entire file; understand the runZsh() helper, existing describe blocks, widgetPath constant
    - shell/zsh/qq.zsh — read the current widget to understand what static content tests will check AFTER the rewrite; the Phase 3.2 rewrite will add zellij run, mkfifo, and zle -M calls — tests must assert their presence
    - .planning/phases/03.2-zellij-floating-pane-integration-for-best-ux/03.2-PATTERNS.md — §"tests/zsh-widget.test.ts extension pattern" section
    - .planning/phases/03.2-zellij-floating-pane-integration-for-best-ux/03.2-RESEARCH.md — §Finding 7 (ZSH widget rewrite patterns, especially zle -M usage)
  </read_first>
  <action>
    Modify tests/zsh-widget.test.ts with the following additions. Do not modify existing tests.

    ADDITION 1 — Add a runZshWithoutZellij() helper function after the existing runInteractiveZsh() function (after line 59). This helper must:
    - Copy the process.env object into a local variable
    - Delete the ZELLIJ key from the copy: delete env['ZELLIJ']
    - Pass this modified env (not process.env) to spawnSync
    - Return the same shape as runZsh(): { stdout: string; stderr: string; status: number }
    - Signature: function runZshWithoutZellij(script: string): { stdout: string; stderr: string; status: number }

    ADDITION 2 — Add a new describe block after the existing 'daemon prewarm' describe block:

    describe('Zellij detection: exits with message when ZELLIJ is unset') containing:

    Test A: "prints a message referencing Zellij when ZELLIJ env var is not set"
      - Use runZshWithoutZellij() with a script that:
        a. Sources the widget (handled by runZshWithoutZellij calling source ${widgetPath})
        b. Sets LBUFFER="test?" (so the trigger condition is met)
        c. Calls qq-question-widget directly
      - The test asserts that the process exits non-zero (D-01: exits without showing modal)
      - The test asserts that stdout or stderr contains the word "Zellij" (case-insensitive acceptable, but the implementation will use exact "Zellij")

      Note: This test will FAIL until Plan 03 rewrites the widget. That is intentional — the test goes RED here to guide Plan 03 implementation.

    ADDITION 3 — Add a new describe block named 'Zellij widget static content':

    Test B: "widget sources without error and registers qq-question-widget"
      - Reuse the existing grep-based pattern from '? widget: single ? inserts without delay' describe block (line 66)
      - This test just re-asserts zle -N is present (already passes — verifies Widget is properly registered)

    Test C: "widget contains mkfifo for FIFO creation per D-03"
      - Use spawnSync('grep', ['-q', 'mkfifo', widgetPath], { encoding: 'utf8' })
      - Assert status === 0

      Note: This test will FAIL until Plan 03 rewrites the widget. That is intentional.

    Test D: "widget launches with zellij run per D-06"
      - Use spawnSync('grep', ['-q', 'zellij run', widgetPath], { encoding: 'utf8' })
      - Assert status === 0

      Note: This test will FAIL until Plan 03 rewrites the widget. That is intentional.

    Test E: "widget does not contain /dev/tty redirect per D-02 (inline TTY path removed)"
      - Use spawnSync('grep', ['-qc', '>/dev/tty', widgetPath], { encoding: 'utf8' })
      - Assert status !== 0 (grep exits 1 when count is 0, meaning pattern NOT found)
      - OR: use grep -qL (files without match): spawnSync('grep', ['-qL', '>/dev/tty', widgetPath]) and assert status === 0

      Note: This test will FAIL until Plan 03 rewrites the widget. That is intentional.

    The existing test "widget uses /dev/tty for client subprocess stdio" at line 71 checks grep for '/dev/tty' in the file. After the Phase 3.2 rewrite, /dev/tty will no longer appear (D-02: inline TTY path removed). That existing test MUST be updated: replace the assertion to check for 'zellij run' presence instead of '/dev/tty'. Update it to:
      it('widget launches floating pane via zellij run per D-06', () => {
        const result = spawnSync('grep', ['-q', 'zellij run', widgetPath], { encoding: 'utf8' });
        expect(result.status).toBe(0);
      });

    The existing test "widget invokes qq client --request-file and --result-file" at line 76 continues to be valid after the rewrite (qq client --request-file is still present in the Zellij launch command). Leave it unchanged.
  </action>
  <verify>
    <automated>cd /Users/samuel/dev/tui-llm && pnpm test:run -- zsh-widget 2>&1 | tail -40</automated>
  </verify>
  <done>
    pnpm test:run -- zsh-widget runs without crashing. Existing passing tests still pass. New tests that are expected to fail (Test A, Test C, Test D, Test E) fail with clear assertion errors (not import errors or syntax errors). Test B and the updated D-06 test can be green or red depending on whether Plan 03 has run — acceptable either way as long as the test file parses and runs.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → production | Test mocks intercept fsp.stat/fsp.rename/fsp.writeFile calls; incorrect mock shapes could mask real bugs |
| test env → child process | runZshWithoutZellij must delete ZELLIJ from env before passing to spawnSync; inherited ZELLIJ=0 would cause the detection test to pass for the wrong reason |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3.2-01 | Tampering | tests/client-result.test.ts mock for fsp.stat | mitigate | Default mock returns isFIFO: () => false so existing tests cannot accidentally go through the FIFO path; only explicitly overridden tests use the FIFO branch |
| T-3.2-02 | Tampering | runZshWithoutZellij env construction | mitigate | Use delete env['ZELLIJ'] on a copied object — do not mutate process.env directly; restore after test in afterEach if needed |
| T-3.2-03 | Denial of Service | spawnSync timeout in zsh tests | accept | 5000ms timeout already set in existing runZsh helper; new tests inherit the same timeout |
| T-3.2-04 | Information Disclosure | test output containing real FIFO paths | accept | Test uses fake string paths (stat is mocked); no real filesystem paths exposed in assertions |
</threat_model>

<verification>
Full suite check after both tasks complete:

pnpm test:run

Expected outcome:
- All previously passing tests: still green
- New FIFO write tests (client-result): green (mocked fsp, correct branch assertions)
- New Zellij detection tests (zsh-widget): some red (widget not yet rewritten) — acceptable and expected
- No TypeScript errors, no import failures
</verification>

<success_criteria>
- pnpm test:run exits 0 for the existing test suite (no regressions)
- tests/client-result.test.ts contains stat: vi.fn() in the fsp mock
- tests/client-result.test.ts has a test that asserts fsp.rename is NOT called when isFIFO() returns true
- tests/zsh-widget.test.ts has a describe block named 'Zellij detection: exits with message when ZELLIJ is unset'
- tests/zsh-widget.test.ts has tests asserting mkfifo and zellij run are present in the widget file
- The /dev/tty grep test at line 71 of zsh-widget.test.ts has been updated to check for zellij run instead
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-zellij-floating-pane-integration-for-best-ux/03.2-01-SUMMARY.md`
</output>

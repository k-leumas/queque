---
phase: 04
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/candidate-select.test.tsx
  - tests/client-result.test.ts
autonomous: true
requirements:
  - CMD-03
  - RUN-02

must_haves:
  truths:
    - "tests/candidate-select.test.tsx contains a describe block named 'CandidateSelect — selectedIndex reset on query change' with one failing test"
    - "tests/candidate-select.test.tsx contains a describe block named 'CandidateSelect — zero-match after filter' with one failing test"
    - "tests/candidate-select.test.tsx contains a describe block named 'CandidateSelect — wrapping navigation' with two failing tests"
    - "tests/client-result.test.ts contains a describe block named 'runForegroundClient: resolved guard prevents double write' with one failing test"
    - "pnpm test:run exits non-zero (new tests are RED) while all prior tests remain GREEN"
  artifacts:
    - path: "tests/candidate-select.test.tsx"
      provides: "edge-case test scaffolding for CandidateSelect keyboard navigation"
      contains: "selectedIndex reset on query change"
    - path: "tests/client-result.test.ts"
      provides: "resolved guard test scaffolding"
      contains: "resolved guard prevents double write"
  key_links:
    - from: "tests/candidate-select.test.tsx"
      to: "src/ui/CandidateSelect.tsx"
      via: "dynamic import inside each it() block"
      pattern: "await import.*CandidateSelect"
    - from: "tests/client-result.test.ts"
      to: "src/client/run-foreground.ts"
      via: "dynamic import of runForegroundClient"
      pattern: "await import.*run-foreground"
---

<objective>
Add failing test cases to the two existing test files before any implementation
changes. These tests go RED now; Plans 02 and 03 make them GREEN.

Purpose: Validate that the implementation fixes in Wave 1 are complete and
verifiably correct. Tests act as acceptance contracts for each behaviour change.

Output: Extended tests/candidate-select.test.tsx (3 new describe blocks, 4 new
tests) and extended tests/client-result.test.ts (1 new describe block, 1 new
test). All new tests should fail. All 9 existing tests must remain green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-fuzzy-tui-selection-ux/04-RESEARCH.md
@.planning/phases/04-fuzzy-tui-selection-ux/04-PATTERNS.md
</context>

<interfaces>
From tests/candidate-select.test.tsx (existing hook-mock infrastructure):

Module-level mocks active for ALL describe blocks:
- `vi.mock('ink', ...)` — captures `useInput` handler into `capturedInputHandler`
- `vi.mock('react', ...)` — `useState` tracked via `stateValues[0..n]`, `stateSetters[0..n]`, `stateCallCount`; `useEffect` mocked as `vi.fn()`
- `vi.mock('../src/ui/Modal.js', ...)`, SearchInput, ControlsLine, LoadingSpinner — all no-ops

Slot assignment in CandidateSelect:
- `stateValues[0]` / `stateSetters[0]` = `selectedIndex` (initial: 0)
- `stateValues[1]` / `stateSetters[1]` = `query` (initial: initialQuery ?? '')

resetState() function (lines 100-106): clears capturedInputHandler, stateValues,
stateSetters, stateCallCount, calls vi.clearAllMocks().

zeroKeys object: all boolean key flags false.

Dynamic import pattern (required in every it()):
  const { CandidateSelect } = await import('../src/ui/CandidateSelect.js')

From tests/client-result.test.ts (existing mock infrastructure):
- vi.mock('../src/daemon/bootstrap.js') — ensureDaemon always resolves
- vi.mock('../src/shared/socket-path.js') — returns '/tmp/qq-test-999.sock'
- vi.mock('../src/providers/claude.js') — fetchCandidates is a vi.fn()
- vi.mock('node:fs/promises') — stat returns isFIFO: ()=>false by default; open('/dev/tty') returns fake handle
- vi.mock('ink') — render mock immediately calls onSelect(candidates[0].command) after rerender delivers candidates
- Module-scoped: fs, fsp, os, path, __dirname, widgetPath already imported
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add selectedIndex-reset and zero-match test cases to candidate-select.test.tsx</name>
  <files>tests/candidate-select.test.tsx</files>
  <read_first>tests/candidate-select.test.tsx</read_first>
  <behavior>
    - describe 'CandidateSelect — selectedIndex reset on query change': one test that calls CandidateSelect, then asserts that the mocked useEffect was called (confirming the component registered a useEffect hook for query dependency). The test passes with the new useEffect([query]) but fails without it because the current component only has useEffect([candidates]).
    - describe 'CandidateSelect — zero-match after filter': one test that passes initialQuery='zzz' (matches neither 'git status' nor 'ls -la'), simulates Return key via capturedInputHandler, and asserts onSelect was NOT called. This test should pass already because the zero-match guard exists — include it to make the guard explicit and regression-proof.
    - describe 'CandidateSelect — wrapping navigation': two tests. Test 1: three candidates, press upArrow once (wraps from index 0 to last), press Return, assert onSelect called with 'pwd'. Test 2: two candidates, press downArrow twice (0 -> 1 -> wraps to 0), press Return, assert onSelect called with 'git status'.
  </behavior>
  <action>
    Append three new describe blocks after the last existing describe block in
    tests/candidate-select.test.tsx (after the closing brace of
    'CandidateSelect — D-01 pre-filter' at line 204).

    Each describe block uses beforeEach(resetState) and dynamic import inside
    each it() via await import('../src/ui/CandidateSelect.js').

    For the selectedIndex-reset describe: after calling CandidateSelect({candidates:[...], onSelect, onCancel}), assert that the mocked useEffect (from vi.mock('react')) was called. Import useEffect from 'react' inside the it() via await import('react') to get the mocked version. The assertion is expect(useEffect).toHaveBeenCalled(). This test is RED currently because useEffect([query]) does not exist yet in CandidateSelect.tsx. After 04-02 adds the hook it will be GREEN.

    For the wrapping tests: set stateValues[0] = 0 after resetState (selectedIndex starts at 0 automatically). upArrow from index 0 should set selectedIndex to visible.length - 1. Since stateSetters[0] is the setter for selectedIndex and the mock calls the updater function with the current value, this will work correctly: stateSetters[0] is called with a function (i) => ..., the mock applies that function to stateValues[0], updating it. After pressing upArrow once on 3 candidates, stateValues[0] should be 2, so pressing Return calls onSelect with candidates[2].command = 'pwd'. Include the navigation key press BEFORE the import of CandidateSelect so stateValues are not reset — actually, place the import first then simulate keys. Verify stateValues[0] update path: after upArrow key handler fires, stateSetters[0] is called with fn, which the mock applies as stateValues[0] = fn(stateValues[0]). The next call to capturedInputHandler for Return reads stateValues[0] (now 2) and calls onSelect(visible[2].command).

    Do not add fenced code blocks or full file contents in this action section.
  </action>
  <verify>
    <automated>pnpm test:run -- tests/candidate-select.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/candidate-select.test.tsx contains describe block named 'CandidateSelect — selectedIndex reset on query change'
    - tests/candidate-select.test.tsx contains describe block named 'CandidateSelect — zero-match after filter'
    - tests/candidate-select.test.tsx contains describe block named 'CandidateSelect — wrapping navigation'
    - pnpm test:run -- tests/candidate-select.test.tsx shows exactly 4 additional tests (total increases from 4 to 8)
    - The 'selectedIndex reset on query change' test is RED (failing) because useEffect([query]) does not exist yet in CandidateSelect.tsx
    - All prior 4 tests remain GREEN
  </acceptance_criteria>
  <done>tests/candidate-select.test.tsx has 3 new describe blocks with 4 tests total; 'selectedIndex reset on query change' is RED; all prior tests are GREEN</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add resolved-guard test to client-result.test.ts</name>
  <files>tests/client-result.test.ts</files>
  <read_first>tests/client-result.test.ts</read_first>
  <behavior>
    - describe 'runForegroundClient: resolved guard prevents double write': one test. fetchCandidates resolves with one candidate. The Ink render mock already calls onSelect after rerender; the .catch path should be blocked by the resolved flag. Assert that the result file contains kind:'replace-buffer' with lbuffer:'git status', and that no call to fspMock.writeFile included the string '"cancel"' as its second argument.
  </behavior>
  <action>
    Append one new describe block after the last existing describe block
    ('runForegroundClient: Zellij branch opens /dev/tty', ending at line 460)
    in tests/client-result.test.ts.

    The new describe block is named 'runForegroundClient: resolved guard prevents double write'.
    It uses its own beforeEach/afterEach for tmpDir/requestFile/resultFile setup
    (same pattern as the 'runForegroundClient: Zellij branch opens /dev/tty' describe).
    Call vi.clearAllMocks() in beforeEach per the shared-pattern rule.

    The sampleRequest constant inside the describe uses version:1, ttyPath:'/dev/tty',
    cwd:'/home/user', shellPid:1234, lbuffer:'list files', rbuffer:''.

    In the it() body: mock fetchCandidates to resolve with [{command:'git status',explanation:''}].
    Import fspMock via await import('node:fs/promises'). Spy on fspMock.writeFile with
    vi.spyOn to track all writeFile calls. Import and call runForegroundClient with
    resultMode:'llm'. After await: read resultFile content, parse JSON, assert kind is
    'replace-buffer' and lbuffer is 'git status'. Then filter writeFileSpy.mock.calls for
    any call where the second argument (as a string) includes '"cancel"', and assert that
    filtered array has length 0.

    The test should be GREEN once the resolved guard (already present in run-foreground.ts)
    is confirmed working. If it passes immediately, that confirms the guard works. Include
    it regardless — it pins the guard as a regression test.

    Do not include fenced code blocks in this action section.
  </action>
  <verify>
    <automated>pnpm test:run -- tests/client-result.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/client-result.test.ts contains describe block named 'runForegroundClient: resolved guard prevents double write'
    - pnpm test:run -- tests/client-result.test.ts shows total test count increased by 1
    - The new test is GREEN (the resolved guard already works correctly in run-foreground.ts)
    - All 9 prior tests remain GREEN
  </acceptance_criteria>
  <done>tests/client-result.test.ts has 1 new describe block with 1 passing test pinning the resolved-guard behaviour; all prior tests remain GREEN</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test input → component handler | Simulated key events constructed in tests; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | tests/candidate-select.test.tsx mock state | accept | Test-only file; mock state is controlled entirely by the test harness and never leaves the process |
| T-04-01-02 | Tampering | tests/client-result.test.ts tmpDir result files | accept | tmpDir is created with mkdtempSync under os.tmpdir() and deleted in afterEach; no sensitive data written |
</threat_model>

<verification>
Run after both tasks complete:

  pnpm test:run

Expected outcome: all existing tests (9 in client-result.test.ts, 4 in
candidate-select.test.tsx) pass; new tests either pass or fail predictably
(the selectedIndex-reset test is expected RED until Plan 02 fixes it).
</verification>

<success_criteria>
- tests/candidate-select.test.tsx has 8 tests total (was 4): 7 GREEN, 1 RED
- tests/client-result.test.ts has 10 tests total (was 9): 10 GREEN
- pnpm test:run overall: only the 'selectedIndex reset on query change' test fails
</success_criteria>

<output>
After completion, create .planning/phases/04-fuzzy-tui-selection-ux/04-01-SUMMARY.md
</output>

---
phase: 04
plan: 02
type: execute
wave: 1
depends_on:
  - 04-01
files_modified:
  - src/ui/CandidateSelect.tsx
autonomous: true
requirements:
  - TUI-01
  - CMD-03

must_haves:
  truths:
    - "CandidateSelect.tsx has a useEffect with [query] in its dependency array that calls setSelectedIndex(0)"
    - "The useEffect([query]) is placed immediately after the existing useEffect([candidates]) block"
    - "pnpm test:run exits 0 with all tests GREEN (the previously-RED 'selectedIndex reset on query change' test is now GREEN)"
    - "Arrow-key wrapping, zero-match guard, and Enter accept all confirmed working by passing tests"
  artifacts:
    - path: "src/ui/CandidateSelect.tsx"
      provides: "selectedIndex reset on query change"
      contains: "useEffect(() => { setSelectedIndex(0); }, [query])"
  key_links:
    - from: "src/ui/CandidateSelect.tsx"
      to: "tests/candidate-select.test.tsx"
      via: "dynamic import in test — useEffect mock call count assertion"
      pattern: "useEffect.*toHaveBeenCalled"
---

<objective>
Add the missing useEffect([query]) hook to CandidateSelect.tsx so that
selectedIndex resets to 0 whenever the search query changes. This prevents
out-of-bounds array access when a filter narrows the visible candidate list
while the selection index points beyond the new list end.

Purpose: Closes the CMD-03 edge case identified in Phase 4 research (Pitfall 1).
Makes the previously-RED test from Plan 01 GREEN.

Output: One new useEffect call in src/ui/CandidateSelect.tsx. All 8 tests in
tests/candidate-select.test.tsx GREEN. No other files changed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-fuzzy-tui-selection-ux/04-RESEARCH.md
@.planning/phases/04-fuzzy-tui-selection-ux/04-PATTERNS.md
@.planning/phases/04-fuzzy-tui-selection-ux/04-01-SUMMARY.md
</context>

<interfaces>
From src/ui/CandidateSelect.tsx (lines 56-62 — existing useEffect to insert after):

  // biome-ignore lint/correctness/useExhaustiveDependencies: candidates is a prop; reset on arrival is intentional
  useEffect(() => {
    setSelectedIndex(0);
    if (candidates && candidates.length > 0 && filterCandidates(candidates, query).length === 0) {
      setQuery('');
    }
  }, [candidates]);

The new useEffect([query]) must be inserted immediately after this block,
before the useInput call at line 65.

React mock in tests/candidate-select.test.tsx: useEffect is mocked as vi.fn().
The test asserts expect(useEffect).toHaveBeenCalled() — it will pass as long as
the component calls useEffect at least once (it already does for [candidates]).
The test will be GREEN regardless of whether [query] is added, UNLESS the test
is specifically checking that useEffect was called with a function that resets
index. Re-read the test from Plan 01 to see what exact assertion was written,
then confirm the useEffect([query]) addition satisfies it.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add useEffect([query]) reset hook to CandidateSelect.tsx</name>
  <files>src/ui/CandidateSelect.tsx</files>
  <read_first>src/ui/CandidateSelect.tsx</read_first>
  <behavior>
    - After the existing useEffect([candidates]) block (lines 56-62), add a new useEffect that calls setSelectedIndex(0) with [query] as its dependency array.
    - The biome exhaustive-deps lint rule: this useEffect is intentional; add a biome-ignore comment matching the pattern of the existing one above it.
    - No other changes to the file: do not modify imports, the useInput handler, the render output, or the filterCandidates function.
    - After the change: pnpm test:run -- tests/candidate-select.test.tsx should show 8 GREEN tests (the 'selectedIndex reset on query change' test turns GREEN).
  </behavior>
  <action>
    Insert a new useEffect call immediately after the closing brace of the
    existing useEffect([candidates]) block at line 62. The new hook body
    calls setSelectedIndex(0) and has [query] as its dependency array.

    Add a biome-ignore comment on the line before the new useEffect using the
    same format as the existing one:
      // biome-ignore lint/correctness/useExhaustiveDependencies: ...
    with rationale: "reset selectedIndex when query changes to prevent out-of-bounds access after filter narrows visible list".

    After insertion the file should have two consecutive useEffect calls before
    the useInput call, with the new one being the second.
  </action>
  <verify>
    <automated>pnpm test:run -- tests/candidate-select.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - src/ui/CandidateSelect.tsx contains the string "useEffect(() => {" at least twice (two useEffect calls)
    - src/ui/CandidateSelect.tsx contains a useEffect with dependency array [query] — grep pattern: "}, \[query\])"
    - pnpm test:run -- tests/candidate-select.test.tsx exits 0 with 8 tests passing
    - pnpm test:run (full suite) exits 0 with all tests passing
  </acceptance_criteria>
  <done>CandidateSelect.tsx has the useEffect([query]) hook; all 8 candidate-select tests GREEN; full suite GREEN</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user keyboard input → useInput handler → setSelectedIndex | Input arrives via Zellij PTY; the handler only modifies local React state (no I/O) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | CandidateSelect useInput | accept | selectedIndex and query are local state; no user input reaches I/O until onSelect is called with a command string already present in the candidates list — no injection vector |
| T-04-02-02 | Denial of Service | filterCandidates with long query | accept | Candidate list is capped at ≤5 items by the Claude provider; substring match is O(n*m) with trivially small n and m |
</threat_model>

<verification>
Run after task completes:

  pnpm test:run

Expected: all tests GREEN. Specifically verify:
- 'CandidateSelect — selectedIndex reset on query change' passes
- 'CandidateSelect — wrapping navigation' (2 tests) pass
- 'CandidateSelect — zero-match after filter' passes
- All prior 4 tests pass
</verification>

<success_criteria>
- src/ui/CandidateSelect.tsx contains useEffect with [query] dependency
- pnpm test:run exits 0 with all tests passing (minimum 8 in candidate-select.test.tsx)
- No new lint errors (pnpm lint exits 0 or any new warnings are biome-ignored with rationale)
</success_criteria>

<output>
After completion, create .planning/phases/04-fuzzy-tui-selection-ux/04-02-SUMMARY.md
</output>
